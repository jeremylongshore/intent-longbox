# intent-longbox environment — copy to .env and fill in. NEVER commit real values.
# Real secrets live in SOPS-encrypted files per the estate standard; this file
# documents the variable NAMES only.

# --- Core ---
# THE VALUES BELOW ARE THE LOCAL DEV STACK, NOT PLACEHOLDERS (E02-B10). They point
# at `docker compose -f docker-compose.test.yml up -d`: port 54329 (not 5432, which
# nothing in this repo ever listens on), database `longbox` (created by
# docker/postgres-init/01-dev-database.sql), and the throwaway role passwords from
# docker/postgres-init/00-roles.sql. Those passwords ARE NOT SECRETS and must never
# become one — the container is bound to 127.0.0.1 for the test lane. Production
# roles are provisioned by the deploy contract with passwords held in SOPS.
# Until this bead the file documented port 5432 and a database nothing created, so
# `pnpm migrate` against a fresh checkout failed twice over.
#
# TWO Postgres URLs, deliberately different roles (E02-D06, 041 §9.2 item 2).
#
# DATABASE_URL is the APPLICATION role. It owns no table and is not a superuser,
# so it cannot `SET session_replication_role` and cannot
# `ALTER TABLE … DISABLE TRIGGER` — which is what makes the append-only guarantee
# (locked decision 4) something the database enforces rather than something the
# server promises. The server asserts this at boot and refuses to serve if the
# role it connects as owns any append-only table.
DATABASE_URL=postgres://longbox_app:longbox_app@127.0.0.1:54329/longbox
# MIGRATE_DATABASE_URL is the SCHEMA-OWNING role. `pnpm migrate`,
# `pnpm grant-app-role` and `pnpm register-shop` use it; nothing serving HTTP does.
# Unset in a non-production shell it falls back to DATABASE_URL with a loud
# warning; with NODE_ENV=production the fallback is refused.
MIGRATE_DATABASE_URL=postgres://longbox_migrate:longbox_migrate@127.0.0.1:54329/longbox
# Optional: the app role's NAME, when a deployment does not call it longbox_app.
# Read by the grant step only; passwords never appear here (values live in SOPS).
# APP_DB_ROLE=longbox_app
# HTTP listen port
PORT=3000
# Directory for uploaded scan photos (v0: local disk).
# NOT SERVED STATICALLY. E03-D05 deleted the public mount (046 §3.3 B5); photos
# leave this box only through the tenant-scoped route
# GET /api/v1/shops/:shopId/scan-sessions/:id/photos/:photoId.
UPLOADS_DIR=uploads

# --- Upload guard (E03-B07) ---
# EVERY VALUE HERE IS A PROVISIONAL SAFETY FLOOR, NOT A MEASUREMENT (042 §8.4's
# class, 021 B16): they are set so ordinary counter work cannot reach them and a
# loop or a bomb reaches them immediately. Never quoted as a supported
# resolution, capacity or volume. Defaults live in src/services/media.ts and are
# what the server uses when these are unset; setting one only TIGHTENS or widens
# a floor deliberately, in an environment where that is written down.
#
# THE SERVER REFUSES TO BOOT if any of these is raised above 4x its default
# (src/config.ts, `assertMediaPolicyOrThrow`). A ceiling that far up does not
# tune the control, it disables it — MEDIA_MAX_PIXELS=99999999999 used to switch
# the decompression-bomb guard off in silence. Lowering any of them is always
# allowed; raising a DEFAULT is a code change with its derivation written down.
#
# The decompression-bomb ceiling. 80 MP is ~1.6x the largest sensor in the pilot
# cohort (035 §9's 50 MP Galaxy A56); the per-side ceiling exists because area
# alone accepts a 1 x 400,000,000 strip.
MEDIA_MAX_PIXELS=80000000
MEDIA_MAX_DIMENSION=20000
# Per-session and per-shop storage ceilings. A session photographs ONE book
# (004), so two dozen frames is several times any real need; the shop ceilings
# are what stop an unattended loop from filling the volume 024 §4 puts uploads on.
MEDIA_SESSION_PHOTO_LIMIT=24
MEDIA_SESSION_BYTE_LIMIT=157286400
MEDIA_SHOP_PHOTO_LIMIT=50000
MEDIA_SHOP_BYTE_LIMIT=53687091200

# --- LLM providers (BYOK) ---
# Default provider key (Anthropic / Claude is the default and reference provider)
ANTHROPIC_API_KEY=
# Pinned default vision model
ANTHROPIC_MODEL=claude-sonnet-5
# OpenAI-compatible provider (optional alternative adapter)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
OPENAI_BASE_URL=https://api.openai.com/v1

# Generic gateway override (jrig Transport lore) — when set, WINS over the
# per-provider settings above and routes the active adapter through it.
LLM_BASE_URL=
LLM_API_KEY=
LLM_MODEL=

# Confidence band thresholds (0..1). high >= BAND_HIGH, medium >= BAND_MEDIUM, else low.
BAND_HIGH=0.85
BAND_MEDIUM=0.5

# --- Pricing (dual sources: PriceCharting historical FMV + eBay live asks) ---
# PriceCharting API token (Premium). Empty = stub client (no live comps).
PRICECHARTING_TOKEN=
# eBay Browse API app credentials (OAuth2 client-credentials; the app token is
# fetched and cached automatically). BOTH empty = stub client (no live asks).
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
# Optional: sandbox host (default https://api.ebay.com) and category filter
# (default 259104 = Comics & Graphic Novels).
# EBAY_BASE_URL=https://api.sandbox.ebay.com

# --- Shopify (per-store Dev Dashboard app for the pilot) ---
# Empty = stub client (no live drafts). Admin API access token, store domain,
# pinned API version.
#
# ⚠ THE STATIC TOKEN IS THE LEGACY PATH FROM E03-B06 ONWARD (046 G-14,
# 000-docs/053 §7.4). It is read ONLY for a shop that has NO connector token
# version at all — the pilot's per-store Dev Dashboard shape — and it is never a
# fallback for a shop whose connector authority was retired: a shop that
# uninstalled the app is REFUSED, not quietly moved back onto this variable.
# The cutover is E16-B02's; its retirement is a later contract step with a 006 row.
SHOPIFY_STORE_DOMAIN=
SHOPIFY_ADMIN_TOKEN=
SHOPIFY_API_VERSION=2025-07

# --- The connector OAuth app (E03-B06, 000-docs/053) ---
# The app's OWN credentials — ONE pair for the deployment, not per shop. They
# stay under 050 §3's rule unchanged (a value a PERSON obtains from a dashboard
# and types into SOPS, so it is an environment variable and never a column), and
# that is the distinction 053 §3.4 turns on: only the per-shop ACCESS TOKEN is
# sealed into a column, because only it is minted by the machine and revocable at
# its issuer without a Longbox act.
#
# Absent = the two connector routes still EXIST and refuse everything. A verifier
# with no key accepts nothing; it does not accept everything, and it does not
# disappear (a 404 would tell an unauthenticated caller which deployments have a
# connector configured).
#
# SHOPIFY_APP_REDIRECT_URI must be the exact `redirect_uri` registered with the
# app: it is the value Shopify sends the merchant back to, and it is what
# `pnpm connector-install` prints into the authorize URL.
SHOPIFY_APP_CLIENT_ID=
SHOPIFY_APP_CLIENT_SECRET=
SHOPIFY_APP_REDIRECT_URI=

# The AEAD ring every connector access token is sealed under (053 §3, 048 R18).
# base64, decoding to EXACTLY 32 bytes: `openssl rand -base64 32`.
#
# ⚠ THE SERVER REFUSES TO BOOT WITHOUT ONE, unconditionally — a check that only
# bites in production is a check no developer ever sees fail. There is no
# plaintext mode and no generate-on-boot mode: a ring minted at startup would
# seal every install made in that process and open none of them after a restart,
# which is a subsystem that appears to work and silently disconnects every shop.
#
# ⚠ IT IS DELIBERATELY A DIFFERENT VARIABLE FROM LONGBOX_AUTHENTICATOR_KEY_V1.
# One key for two subsystems means one compromise is two — and it would make the
# one destruction Longbox can actually perform (destroying a key version so a
# stored ciphertext can never be opened again) an act that also takes every
# owner's second factor with it.
#
# ROTATION IS ADDITIVE, exactly as the authenticator ring's is: every version
# present DECRYPTS, the highest present ENCRYPTS, so introducing V2 is a variable
# and a restart with no row touched. Removing V1 before its rows are re-sealed
# takes those installs with it — the way back is a new install, not a recovery
# code, because a connector token has no second factor.
LONGBOX_CONNECTOR_KEY_V1=
# LONGBOX_CONNECTOR_KEY_V2=

# --- Outbox drain (E02-D07, 043 §5.3, §7.1) ---
# FOUR PROVISIONAL FLOORS AND ONE INTERVAL. These are NOT measurements, are
# never quoted as reliability, throughput or latency in any artifact at any
# class (021 B16), and support no claim of fact. They are circuit-breaker
# floors adopted under 042 A3 ("018 A3 caps claims of fact, not safety
# floors"). They close by measurement across the 019 §5 pilot batches,
# SEGMENTED by whether a real Shopify credential was configured — a stub client
# never fails, so a mixed sample would report a reliability belonging to the
# stub. 018 C3's red line: a ceiling may be RAISED freely because it binds
# less; LOWERING one after seeing a result it would change needs a 006 row.
# Unset everywhere = the defaults in src/services/outbox.ts.
# OUTBOX_MAX_ATTEMPTS=6
# OUTBOX_BACKOFF_BASE_MS=30000
# OUTBOX_BACKOFF_CEILING_MS=1800000
# ATTEMPT VISIBILITY: the in-flight window. ⚠ It must exceed the worst-case
# CLOCK SKEW across every host that runs a worker (043 A7) — every timestamp in
# the design is server-side now(), and beyond a single host NTP or a monotonic
# source is a requirement, not an operational nicety.
# OUTBOX_ATTEMPT_VISIBILITY_MS=300000
# OUTBOX_POLL_INTERVAL_MS=5000
# OUTBOX_BATCH_SIZE=10

# --- Webhook replay and the privacy clock (E03-B08, 000-docs/064) ---
#
# THREE MORE PROVISIONAL FLOORS, under the same 018 C3 rule as the six above: a
# ceiling may be RAISED freely because it binds less; LOWERING one after seeing a
# result it would change needs a 006 row. Unset everywhere = the defaults in
# src/services/connectors/shopify/policy.ts. None of them is a measurement and
# none may be quoted as one.
#
# RECEIPT WINDOW: how old a signed message may be and still be acted on, and how
# far back a repeat of the same BYTES counts as a redelivery rather than a new
# event. 48 hours, derived from the provider's own retry horizon (064 §4.1) —
# refusing beyond it costs nothing a legitimate retry would have delivered. Its
# closing evidence is the first week of real delivery ages.
# CONNECTOR_WEBHOOK_RECEIPT_WINDOW_SECONDS=172800
#
# CLOCK SKEW: the guard band on the uninstall's event-time bound (064 §5.2). ⚠ It
# is the SAME class of cross-clock comparison OUTBOX_ATTEMPT_VISIBILITY_MS makes,
# and 043 A7's condition binds it identically — NTP is a requirement, not an
# operational nicety. The band's DIRECTION is the decision: with it, skew retires
# one token too MANY (the shop re-installs); without it, one too FEW (a token this
# system believes is live after the merchant killed it at Shopify).
# CONNECTOR_WEBHOOK_CLOCK_SKEW_SECONDS=300
#
# PRIVACY FULFILMENT WINDOW: how long after a privacy message arrives before
# `pnpm audit:privacy-requests` calls it OVERDUE and exits non-zero.
# ⚠ IT IS NOT A LEGAL DEADLINE AND NO ARTIFACT MAY PRESENT IT AS ONE. What a
# person is entitled to, on what clock, and what they are told is E03-B09's bead
# and E01-B06's counsel engagement. This number exists so that "nobody answered"
# is detectable, and its only claim is about this repository's own attention.
# PRIVACY_REQUEST_FULFILMENT_WINDOW_DAYS=25
#
# ⚠ IT IS READ. At v1.0.0 of this bead it was documented HERE and read by NOTHING
# (the invariant review's finding 2), so an operator who set it got the default
# and no error — a false statement in the file operators trust most. It now
# travels through `loadWebhookWindows` and the composition root like the two
# above. Its DERIVATION (064 §6.1): deliberately shorter than any external clock
# this system might be measured against, so a finding fires EARLY; within that
# rule the integer is arbitrary and is stated as arbitrary. Note the unusual
# direction of 018 C3 here — LOWERING it binds MORE and is free; raising it past
# whatever counsel eventually says needs a 006 row.
#
# UNRESOLVED-DOMAIN CEILING: how many compliance obligations may accumulate for
# ONE store domain that matches no install (064 §7.5, the security lens's F5).
# It bounds a TOTAL and not a rate, because the harm is a permanently non-zero
# `pnpm audit:privacy-requests` exit rather than a burst — a rate limiter refuses
# for a minute and then forgets. A tenant-RESOLVED obligation is never bounded by
# it. Derivation: a store matching no install produces at most one `shop/redact`
# per offboarding (053 §5.5's own example), and eight is that number with room
# for the provider's retries and for a store that offboards more than once.
# ⚠ The trade is stated as 064 §9 R8: past the ceiling a genuine late message is
# still RECORDED in the receipt, and its clock does not start.
# PRIVACY_UNRESOLVED_DOMAIN_CEILING=8

# --- Shopify dev-store upsert-consistency check (A11, OPEN) ---
# 043 A11 signs ONE assumption OPEN: that a `customId` written by a productSet
# CREATE is immediately findable by a productSet upsert issued moments later. A
# metafield-backed lookup that were eventually consistent would make a fast
# retry create a SECOND product — the exact failure the key exists to prevent —
# and no citation can settle it, because the question is about behaviour and
# not documentation. It closes by MEASUREMENT against an ISOLATED DEV STORE,
# before the saga serves a real shop.
#
# Set all three and run `pnpm test:integration` to execute
# tests/integration/shopify-customid-consistency.test.ts; it SKIPS otherwise.
# ⚠ NEVER point these at a live shop: the test creates a real product.
# SHOPIFY_DEV_STORE_DOMAIN=
# SHOPIFY_DEV_ADMIN_TOKEN=
# SHOPIFY_DEV_API_VERSION=2025-07

# --- Per-shop credential overrides (BYOK seam) ---
# shop_credentials.key_ref stores the NAME of an env var, never a raw key.
#
# E03-D01 (046 §5 A7/A15) MADE THE NAMING A RULE RATHER THAN A CONVENTION. Every
# key_ref must be one of SIX names per shop — `LONGBOX_<SLUG>_` followed by
# ANTHROPIC_KEY, OPENAI_KEY, SHOPIFY_KEY, PRICECHARTING_KEY, EBAY_KEY or
# EBAY_KEY_SECRET — enforced by the CHECK in migrations/014, by the Zod contract,
# and by `resolveKeyRef(keyRef, shopSlug)`, which tests EXACT MEMBERSHIP before it
# reads the environment. The older `SHOP_<SLUG>_<KIND>` names are gone; re-run
# `pnpm register-shop` for any shop still carrying one.
#
# ⚠ THE SLUG'S SEPARATORS ARE STRIPPED, NOT UNDERSCORED: shop `gotham-city` owns
# `LONGBOX_GOTHAMCITY_…`, never `LONGBOX_GOTHAM_CITY_…`. That second spelling was
# a real defect — it let shop `gotham` read shop `gotham-city`'s variable through
# a prefix test — and it is now refused at every layer.
#
# THERE IS NO GLOBAL FALLBACK FOR A SHOP THAT HAS CREDENTIAL ROWS. Leaving one
# of these unset makes the vision provider REFUSE for that shop and the
# Shopify/PriceCharting/eBay clients STUB. The global vars above are the default
# for a shop that declares NO row at all — never a silent substitute for a shop's
# own key.
# LONGBOX_GOTHAM_ANTHROPIC_KEY=
# LONGBOX_GOTHAM_SHOPIFY_KEY=
# LONGBOX_GOTHAM_PRICECHARTING_KEY=
# eBay is a credential PAIR: the key_ref names the client-ID var; the secret
# lives at `${key_ref}_SECRET` (still names-in-DB, raw keys only in env/SOPS).
# LONGBOX_GOTHAM_EBAY_KEY=
# LONGBOX_GOTHAM_EBAY_KEY_SECRET=
#
# E03-B05 (050 §2 Q1, §4) ADDS A LIFECYCLE ABOVE THE NAME, AND CHANGES WHERE THE
# VALUE MAY COME FROM. Two things a reader of this file has to know:
#
#   1. **The variable is still the process-visible form, and its ONLY legitimate
#      source is a SOPS-encrypted file decrypted at every (re)start into a
#      mode-0600 root-owned `EnvironmentFile` ON TMPFS** — `/run/intentsolutions/
#      longbox.env` under a `RuntimeDirectory=`, or `/dev/shm`. **Never under
#      `/etc` or `/var`**: a persistent plaintext copy survives a reboot, a
#      snapshot and a backup pass, reaching the borg repository, the VPS replica
#      and then Backblaze B2 under Object Lock — a 30-day-undeletable copy of a
#      key the shop believes it revoked. `/run` and `/dev/shm` are outside every
#      backup source root by construction, so no exclude rule has to be
#      maintained for them. That rendered file is NOT a backup: never copy,
#      archive, `scp` or commit it. The SOPS file is the only copy of record; an
#      operator who needs the value again re-runs the decrypt. (The unit file
#      itself — the `RuntimeDirectory=`, the `ExecStartPre=` decrypt and the
#      mandatory `EnvironmentFile=` with no leading `-` — is E13's, per 050 §13
#      item 7.)
#
#   2. **A shop with any credential VERSION and no LIVE one is refused, not
#      fallen back.** Setting a variable is no longer enough on its own: the
#      resolver reads `shop_credential_version` minus `shop_credential_retirement`
#      (`pnpm register-shop` writes the first version). Retiring the last version
#      makes the credential unreachable to this process WHILE THE VARIABLE IS
#      STILL SET — deliberately, so an offboarding whose SOPS edit slips a day is
#      a shop that cannot make calls rather than a shop whose key is quietly
#      still live. **Longbox cannot revoke a BYOK key; only the shop can, at the
#      provider.**

# --- Metered spend ceilings, per OWNER (E03-B05, 050 §2 Q4(c), §6.4) ---
# Paid identify calls per shop per day. TWO numbers, because a shop spending its
# OWN money and a shop on Longbox's SERVICE ACCOUNT are not the same risk: the
# second can spend an unbounded amount of somebody else's money by looping.
#
# ⚠ BOTH ARE PROVISIONAL CIRCUIT-BREAKER FLOORS (042 §8.4), NOT MEASUREMENTS.
# Neither is ever quoted as capacity, cost, throughput or reliability in any
# artifact at any class (021 B16). A floor may be RAISED freely; LOWERING one
# after seeing a result it would change requires a 000-docs/006 row saying so in
# those words (018 C3).
#
# The server REFUSES TO BOOT on a value more than 4x its default — a ceiling that
# far above the default disables the control rather than tuning it, and refusing
# is louder than clamping. ZERO IS LEGAL and means every identify call takes the
# manual path, which is a route the operator already has.
# LONGBOX_METERED_BUDGET_SHOP=500
# LONGBOX_METERED_BUDGET_SERVICE_ACCOUNT=150

# --- Gateway override scope (E03-D01, 046 §11 I5) ---
# LLM_BASE_URL + LLM_API_KEY outrank EVERY per-shop credential, so the pair is
# validated at boot: set both or neither, and the host must be a registered
# provider host (api.anthropic.com, api.openai.com, api.ebay.com,
# api.sandbox.ebay.com). A self-hosted gateway is added here explicitly —
# comma-separated hosts — rather than by disabling the check.
# LONGBOX_GATEWAY_HOST_ALLOWLIST=gateway.internal.example

# --- Identity and sessions (E03-D09, E03-D11; 048 §9.2 R6, §5.1 R9) ---
#
# LONGBOX_PIN_PEPPER — REQUIRED. The server refuses to boot without it.
#
# ⚠ THE NAME IS NARROWER THAN THE JOB, AND THE JOB IS NOW THREE CREDENTIALS.
# This ONE value is mixed into every operator PIN, every PASSWORD (E03-D11) and
# every RECOVERY CODE, as argon2id over (secret ‖ pepper). 048 §9.2 rules one
# value for the two factors it names and 055 §9 item 11 hands the confirmation to
# E03-D11, which took it: two peppers would be two custody obligations, two
# things to lose and two rotations to run, in exchange for a separation that buys
# nothing — see the paragraph below. The variable keeps its name because renaming
# a boot-required secret is a deploy-coordination step with a real outage window
# and no security gain; the rename rides the versioned pepper ring 055 §9 item 5
# asks for (000-docs/057 §9 residual R3).
#
# It is read from the process environment and NEVER written to the database, to a
# migration, to a fixture or to a backup of this database. For a six-digit PIN it
# is close to load-bearing: a million-entry keyspace is enumerable offline
# against argon2id in time that is merely expensive, and 048 §3.5's whole
# "possession-bound channel" argument evaporates the moment the verification can
# be done offline — because a pg_dump is not a phone behind a counter. For a
# password it is defence in depth.
#
# ⚠ **RECOVERY CODES ARE NOT A FALLBACK FOR A CUSTODY LOSS, ONLY FOR A LOST
# PHONE.** That sentence is 055 §9 item 4a's own, reproduced here because it is
# the condition 055 attached to the branch this system took (000-docs/057 §4.7):
# the codes keep the pepper, so they die with it.
#
# ⚠ LOSING IT STOPS EVERY SIGN-IN IN EVERY SHOP, AT ONCE — every PIN, every
# password and every recovery code becomes unverifiable in the same instant, and
# there is NO way back in from inside the system, because every path back in is
# authenticated by something under the lost value (055 §2 E4/E6). In particular:
# THE RECOVERY CODES DO NOT SURVIVE IT. They are hashed with this pepper too, and
# 048 R20 makes a recovery code a SECOND factor that is never accepted without
# the password — which is also hashed with it. The way back is 055 §6.1's
# runbook, and 055 RULING 6 is the sentence a shop is owed in its own terms.
#
# That makes it a backup and custody obligation, not a value to regenerate when a
# deploy complains. 055 RULING 1 adopts escrow for it; 055 RULING 4 records that
# the escrow DOES NOT EXIST YET and that the live posture is the accepted-risk
# interim. Generate with `openssl rand -base64 48`; store in SOPS.
# LONGBOX_PIN_PEPPER=
#
# LONGBOX_AUTHENTICATOR_KEY_V1 — REQUIRED. The server refuses to boot without it.
#
# The AES-256-GCM key every TOTP secret is sealed under (048 §4.2, R18, E03-D06).
# Base64 of exactly 32 bytes: `openssl rand -base64 32`. Store in SOPS; render it
# into the mode-0600 tmpfs EnvironmentFile 050 §3 specifies, never onto persistent
# disk, and never treat that rendered file as a backup.
#
# A TOTP secret is the one credential this system MUST be able to read, so locked
# decision 2's "raw keys never in the database" cannot be satisfied by naming an
# environment variable per person. 048 §4.2 rules the split instead: the secret is
# encrypted at rest with a key held OUTSIDE the database, so a database compromise
# alone is not a credential compromise. Each row carries a per-row nonce, its own
# `key_version`, and the row's id as additional authenticated data — so a ciphertext
# moved between rows fails to authenticate rather than decrypting to a working
# second factor.
#
# ROTATION IS ADDITIVE, AND THAT IS THE POINT OF `key_version`:
#   1. add LONGBOX_AUTHENTICATOR_KEY_V2 and restart. New enrollments seal under V2;
#      every existing row still opens under the V1 key it names. Nothing is
#      re-encrypted and no deploy has to touch the table.
#   2. re-encrypt the V1 rows — `pnpm reencrypt-authenticators` (E03-D11), a job
#      that produces FACTS: per person, a `replaced` retirement plus a NEW
#      authenticator row sealed under V2, in one transaction, never an in-place
#      rewrite of a secret column (048 R18 binds the ciphertext to the ROW's id,
#      so a rewrite would carve an exception into the property that makes a moved
#      ciphertext fail). THE SECRET IS UNCHANGED and `last_used_step` is carried
#      forward, so nobody re-scans anything and no code becomes replayable.
#      `--dry-run` prints the worklist and writes nothing.
#   3. only when it reports zero remaining, remove LONGBOX_AUTHENTICATOR_KEY_V1.
#
# ⚠ REMOVING A KEY BEFORE STEP 2 takes every second factor still sealed under it.
# The way back in is 048 §8's recovery codes — BUT ONLY IF THE PEPPER SURVIVED.
# If the SOPS file itself was lost, the codes went with it (055 §2 E4) and the
# answer is 055 §6.1. (This sentence is 055 §9 item 4b's, applied verbatim: the
# earlier text said the recovery codes were the way back with no condition, which
# is true for a key REMOVED in a botched rotation and false for a file loss.)
# Losing every key with no recovery codes left is 048 §8.2's Longbox-operated
# break-glass, not a support ticket.
# LONGBOX_AUTHENTICATOR_KEY_V1=
#
# LONGBOX_PUBLIC_ORIGIN — the origin(s) this deployment answers on, comma
# separated. It is the FALLBACK half of 048 §5.1's CSRF check: the primary
# mechanism is `Sec-Fetch-Site: same-origin`, and this list is consulted only
# when a request carries no `Sec-Fetch-Site` at all (an older browser, a
# non-browser client). Leaving it empty makes that fallback CLOSED, not open.
# LONGBOX_PUBLIC_ORIGIN=https://longbox.example
#
# LONGBOX_BUILD_SHA — the commit this build was made from (E03-B03, 054 §4.2).
# It is stamped on every `authorization_decision` row beside the matrix semver,
# because a semver names a CONSTANT and two deployments can both say `1.0.0`
# while one carries an edit nobody bumped. Unset, the column records the string
# `unknown`, which is an honest answer rather than a null; the deploy sets it.
# It is NOT a secret and NOT a control — it improves an audit and enforces
# nothing, which is why the server starts without it.
# LONGBOX_BUILD_SHA=0000000000000000000000000000000000000000
#
# --- Local bootstrap ONLY (development; refused when NODE_ENV=production) ---
#
# 034 §4.5 is explicit that onboarding creates no device: a phone is enrolled by
# the people who own it, and seeding a fake "counter phone" would put rows in a
# shop's data nobody at the shop made. Setting LONGBOX_BOOTSTRAP_PIN opts a LOCAL
# `pnpm register-shop` run into enrolling one phone and giving the owner that PIN,
# so a fresh database can run the scan flow at all — without it every shop-scoped
# route is behind a device session nothing can issue, and the first thing anybody
# would do is comment out the authentication hook.
#
# The PIN must satisfy the shop's own rule (six digits, no run, no repeat), and
# the device secret is printed ONCE by the script: the database stores only its
# sha256, so losing it means enrolling again. The real flow — a one-time,
# short-lived, shop-scoped enrollment code redeemed ON the device — is E03-D07's.
# LONGBOX_BOOTSTRAP_PIN=
# LONGBOX_BOOTSTRAP_DEVICE_LABEL=local bootstrap phone
