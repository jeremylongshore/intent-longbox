# The beads pre-commit hook is deliberately NOT chained: it re-exports
# .beads/issues.jsonl, and bead data stays out of this public repository.

pnpm lint-staged
pnpm typecheck && pnpm test
# Containment via in-repo audit-harness — never ~/.claude paths.
pnpm exec audit-harness escape-scan --staged
pnpm exec audit-harness verify
