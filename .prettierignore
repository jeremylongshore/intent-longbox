node_modules/
dist/
coverage/
uploads/
pnpm-lock.yaml
000-docs/
migrations/
CHANGELOG.md
LICENSE
.beads/
.agents/
.claude/
.codex/
TEST_AUDIT.md
# Generated, gated diagram output from the workflow-diagram-design skill (doc 045).
# It is a rendered artifact, not authored source: reformatting it would change bytes
# the skill's own gates signed off. Added by E02-B10 because it broke `pnpm lint`
# on main, which is a required check this branch has to pass.
.arch/
# The GENERATED OpenAPI artifact (042 §2.5(c), E02-D08). Its bytes are the
# contract: `pnpm contracts:emit` writes it and a contract test asserts the
# committed file equals a regeneration, so a formatter rewriting it would fail
# that gate on every commit while changing nothing anybody declared.
contracts/openapi.v1.json
