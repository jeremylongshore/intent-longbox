# 000-docs Index: intent-longbox

**Version:** 1.34.4

**Last Updated:** 2026-09-05

| # | Doc | Hook |
|---|-----|------|
| 002 | [002-PP-PRD-product-requirements.md](002-PP-PRD-product-requirements.md) | Numbered requirements (R1 to R20, MoSCoW-tagged): scan-session flow, barcode-first, evidence gate, confidence bands, condition-never-numeric, DRAFT-only Shopify. |
| 003 | [003-AT-ARCH-architecture.md](003-AT-ARCH-architecture.md) | Panel-hardened architecture: the pipeline, the Hickey immutable data model, the deterministic vs. probabilistic boundary, and the open build-vs-buy retrieval gate. |
| 004 | [004-PP-UJRN-user-journey.md](004-PP-UJRN-user-journey.md) | The three journeys in plain retail language: employee at the long box, owner reviewing drafts, and what happens when a call is wrong. |
| 005 | [005-AT-SPEC-technical-spec.md](005-AT-SPEC-technical-spec.md) | Concrete v0 spec: Postgres schema sketch, API surface, provider adapter shape (BYOK), Shopify productSet integration, eval and cost hooks, Whatnot CSV roadmap. |
| 006 | [006-OD-STAT-status.md](006-OD-STAT-status.md) | Where things stand: plan approved 2026-09-01, Phase 0 item states, Phase 1 in progress. |
| 018 | [018-AT-DECR-longbox-evidence-rules-2026-09-03.md](018-AT-DECR-longbox-evidence-rules-2026-09-03.md) | Decision record (E00-B03, RATIFIED 2026-09-03): the evidence ladder and register states, source precedence (inspected code outranks status prose; measurements outrank marketing), contradiction and supersession rules, and change control for locked decisions, thresholds, registers, test policy and external claims. Restricted internal. |
| 022 | [022-AT-DECR-longbox-human-authority-and-workplace-principles-2026-09-03.md](022-AT-DECR-longbox-human-authority-and-workplace-principles-2026-09-03.md) | Human-authority, labor, accessibility, privacy and non-surveillance principles P1–P9 (E00-B06, RATIFIED 2026-09-03 via council): enforcement class per principle, Q1–Q7 decided, T35 origin, binding minority constraints absorbed. Restricted internal until the one-pager ships. |
| 023 | [023-OD-STND-longbox-beads-configuration-routing-and-evidence-closure.md](023-OD-STND-longbox-beads-configuration-routing-and-evidence-closure.md) | Beads configuration, routing and evidence-closure standard (E00-B07): store/hooks/backup facts, alias + metadata rules, 014 §11.3 routing, the eight formulas, the close-with-evidence rule, restore drill, per-environment checklist. Restricted internal. |
| 024 | [024-AT-ARCH-longbox-stack-and-artifact-map-2026-09-03.md](024-AT-ARCH-longbox-stack-and-artifact-map-2026-09-03.md) | Stack and artifact map (first draft of the E13-B01 topology ADR): where every artifact lives today vs planned, backup/retention per artifact, runtime topology, why Immich is not a stack component, six gaps with owning beads. MAPPED, NOT BUILT. Restricted internal. |
| 026 | [026-AT-DECR-longbox-g0-truth-lock-sign-off-2026-09-03.md](026-AT-DECR-longbox-g0-truth-lock-sign-off-2026-09-03.md) | G0 Truth Lock sign-off (E00-B08): the dated decision, the G0 criteria ↔ artifacts table, the restorable baseline manifest (git tag `g0-truth-lock`, Dolt backup commit, doc hashes), what G0 does not assert. Restricted internal. |
| 027 | [027-AA-AUDT-longbox-repository-truth-audit-addendum-to-013-2026-09-03.md](027-AA-AUDT-longbox-repository-truth-audit-addendum-to-013-2026-09-03.md) | Repository truth audit (E02-B01), an addendum to 013 at pinned commit `46d9910`: all 176 of 013's EXISTS citations resolved and re-read, every claim in 013, 005, 006 and the 014 §2.2 gap map re-stated as VERIFIED / CONTRADICTED / OPEN with file:line, 23 contradictions (11 substance, 12 citation-only), the 016/000-INDEX drift list, the docs-to-correct queue, and the in-repo identity repair. Restricted internal. |
| 029 | [029-AT-DECR-longbox-modular-monolith-boundaries-and-extraction-criteria-2026-09-03.md](029-AT-DECR-longbox-modular-monolith-boundaries-and-extraction-criteria-2026-09-03.md) | Decision record (E02-B02, **RATIFIED 2026-09-03** via a two-lens architecture cannon): nine modules in one Fastify service (identity=tenancy+auth, workflow, catalog, resolution, condition, valuation, commerce, reporting, platform) with every table owned exactly once (17 at v1.1.1; 56 at v1.2.0, of which 39 are not yet created), a layered dependency rule with **no L2→L2 edges** as a mermaid graph plus the `.dependency-cruiser.cjs` that becomes the E02-B10 gate-test staged to modules holding real code, provider containment with the five current violations named at file:line, a 13-step no-behaviour-change migration off the flat `src/services` layout at one commit per relocation, per-module extraction triggers anchored to T10/T13a/T21/T22 and OPEN until PILOT-MEASURED, §12 naming the request-scoped transaction as the temporary consistency mechanism until the E02-B09 outbox, four alternatives, the five cannon questions with their answers, and Fowler's preserved dissent on the module count. v1.1.1 repairs four gate-audit factual blockers (the insert paths, the L2 `identity` inconsistency, `reporting`'s absence from the staged gate, §12's tense) with no decision changed. **v1.2.0 applies the eight amend-by-a-row entries 034 §7 (three), 036 §7.5 (four) and 030 §7 (one) deferred to or implied for E02-B05** — the `identity`, `workflow`, `catalog`, `commerce` and `reporting` owned-table lists grow by 39 tables, §2.2 and §2.7 each gain the one sentence their entry instructs, §2.3's catalog list stops reserving the LCID/edition/signature/crosswalk tables in prose and names them, and §2.10's map goes from 17 rows to 56 with `physical_item_active_listing` flagged as the one non-append-only table and `vertical_pack` as insert-only; no boundary, edge, rule or extraction criterion changed and every added table is ASSERTED and unwritten. **v1.3.0 applies the two entries 047 §12.4 item 6 and 016 C39 (E04-D02):** `catalog` owns fourteen tables and all of them now exist — the eight from 030 §7 plus the five LCID lifecycle rows (`lcid_merge`, `lcid_split`, `lcid_split_outcome`, `lcid_retirement` and `lcid_current_survivor`, the last being the one projection exemption) and `corpus_version`, landed by `migrations/016` and `017` at E04-D01; `identity_resolution` stays workflow-owned; §2.10 goes from 56 rows (17 created) to 61 (31 created) and names the platform tables from `005`/`009`/`010`/`011` that it still does not map as a separate amend-by-a-row; and §3.3 gains a patch row making the INSTALLED `.dependency-cruiser.cjs` governing — defect N4 (`^pg$` never matched a resolved npm path, so the rule was green by construction; fixed at `510e4b8`) plus the two named `catalog-public-surface-only` / `catalog-is-a-leaf` rules by which `catalog` joined the staged gate early. **v1.3.1 closes the gap v1.3.0 named (E02-D12):** §2.10 gains six rows — `listing_status_observation` (commerce, `005`), `request_idempotency` (platform, `009`, declared exemption), `scan_session_transition` (workflow, `010`), `outbox` and `outbox_attempt` (platform, `011`), `schema_migrations` (platform ledger, `scripts/migrate.ts`, declared exemption) — every owner already fixed by 040, 042, 043, 044 and `src/db/appendOnlyTables.ts`, so no module gains a table it did not already own and the bump is a patch; §2.10 goes from 61 rows (31 created) to 67 (37 created). **v1.4.0 applies 048 §12.4 row 9 (E03-D09):** `identity` gains four tables — `app_session`, `app_session_revocation`, `operator_pin` and `auth_attempt` — and eight it already named (`organization`, `location`, `app_user`, `membership`, `membership_revocation`, `device`, `device_credential`, `device_credential_revocation`) move from *(sketch, 034 §4.1) — not created* to `migrations/019`/`020` at a line; §2.1 gains one paragraph stating `auth_attempt`'s SUBSTRATE obligation (048 R17) as the fourth name §3.3's accessor rule must gate; §2.10 goes from 67 rows (37 created) to **71 (49 created)**. A minor, by v1.2.0's own test, because an owned-table list grows. Restricted internal. |
| 030 | [030-AT-DECR-longbox-collectible-edition-and-vertical-pack-contracts-2026-09-03.md](030-AT-DECR-longbox-collectible-edition-and-vertical-pack-contracts-2026-09-03.md) | Decision record (E02-B04, **RATIFIED 2026-09-03** via a two-lens cannon, v1.3.1 — **v1.3.1 is an E04-B04 patch: §5.4's card sketch offers `MINT` and `GEM` bands, 021 gained B21 (*"'Mint' as a Longbox band"*, on 037 §2.0.5's Mile High Comics quote) AFTER the sketch was written, and pack certification now refuses either band in every vertical while the sketch's text is left as written with a note beneath it — no decision changed, and the authoritative card vocabulary is still E08's (049 §8);** **v1.3.0 is an amend-by-a-row taken at 049's ratification under 030 §11's standing revision right, exercised by the acting head: `parallel` enters the card edition signature and `printRun` joins the card edition attribute list, deliberately outside the signature (049 §5.2);** **v1.2.1 is the E04-B01 gate-audit patch: §3.3's card-signature bullet aligned with the v1.2.0 §5.4 amendment, so the record no longer carries two card signatures (a patch — no decision changed);** **v1.2.0 is an amend-by-a-row taken at 047's ratification under 047 A6: §5.4's card signature loses `grader`, `grader`/`cert_number` move out of the card edition into the copy-level record symmetric with 047 §8.4, and `psa_cert` leaves the card pack's crosswalk namespaces; E04-B03 carries the acceptance line that cert namespaces are excluded from `edition` and `edition_signature`**; v1.1.1 repaired two gate-audit factual items (§5.1 comment, contract-field count) with no decision changed): the core chain `collectible_definition → edition → physical_item` with definitions and editions as versioned immutable values and the copy as an identity, the reference rule that every outside table points at an LCID and never at a row, the two supersession mechanisms (corpus advance vs in-corpus correction), the recommendation of a `vertical` discriminator + JSON-Schema-validated `attributes jsonb` over per-vertical extension tables with the four query patterns that decide it and a named escape hatch to option (c), the vertical-pack manifest as a TypeScript interface and as `vertical_pack`/`vertical_pack_version` rows, the shipped comic pack mapped slot-by-slot onto today's code at file:line, a card pack sketched to prove zero core forks, external IDs as rights-bearing crosswalk edges under 019 T25, an expand-only migration sketch for E02-B07/E02-B10, 13 invariants mapped to future test files, five alternatives, consequences, and the five cannon questions with their answers. v1.1.0 absorbs eight cannon amendments (`rich-hickey-reviewer` + `martin-fowler-reviewer`, both ACCEPT-WITH-CHANGES, none declined): the reserved nullable `confirmed_edition_lcid` column is replaced by the append-only `identity_resolution` fact table, `lcid_registry(lcid)` becomes a real FK target (v1.0.0 wrongly conflated FK-to-`edition.id` with FK-to-registry and gave away referential integrity for nothing), the manifest is trimmed to the seven contract fields the comic pack populates with `listingTemplate`/`evalSlices` reserved-not-fields, `edition_signature` stays a table on recoverability, `packVersion` is single-rate, invariant I1's deny-list adds `upc\|ean\|isbn` and derives provider names at runtime, E04-B11 gains a measured import-validation bound making the option-(a)-over-(c) trade falsifiable, and Fowler's dissent on both `edition_signature` and the pack machinery's timing is preserved. Restricted internal. |

| 033 | [033-AT-BLUE-longbox-service-blueprints-2026-09-03.md](033-AT-BLUE-longbox-service-blueprints-2026-09-03.md) | The four service blueprints (E01-B03, v1.0.0): one table per actor — employee scanning a long box, owner reviewing drafts and the weekly email, Longbox support handling a stuck session or a dispute, and a pricing/catalog provider degrading — each lane-encoded (physical evidence · frontstage · line of interaction · backstage · line of visibility · support process · systems `today file:path → planned bead` · failure path · 017 pain with severity · which immutable record is written); eight explicit failure paths (poor/no Wi-Fi with nothing queued today, interruption parked/resumed/abandoned, low-band manual dead end, contradiction removing the one-tap path, provider outage in stub mode, owner rejection flowing back as a superseding record, walk-in consent revocation on the 5-business-day tombstone clock, and a buyer-surfaced wrong call recorded as a system defect and never an employee fault); the handoff matrix with the consent/rights condition on every edge; the owner-review path in detail (identity, condition words, price source, decision strip C4, provenance C5; publish / edit / send back and what each writes); §8 what changes versus 004 with 004 marked superseded-for-design; §9 design implications tied to E05-B01/B02/B04/B08/B09, E08-B01, E09-B08, E10-B04/B08, E11-B02/B03/B06/B09, E16-B03/B08. Restricted internal. |
| 034 | [034-AT-DECR-longbox-tenancy-and-work-context-model-2026-09-03.md](034-AT-DECR-longbox-tenancy-and-work-context-model-2026-09-03.md) | Decision record (E02-B03, **RATIFIED 2026-09-03** via a two-lens cannon, **now v1.2.0** — v1.1.0 absorbed eight amendments, none declined; v1.1.1 patched four statements of fact; **v1.2.0, 2026-09-04, applies 048 §7.4's amend-by-a-row: `device_credential.key_ref` becomes `token_hash text NOT NULL`, `sha256` of a 256-bit system-minted secret, with I4 rewritten to assert what the column must be. The v1.0.0 text had applied locked decision 2's "the NAME of a reference, never a key" rule verbatim to an object it does not fit — a device credential is minted by the system one per phone, so there is no env var to name and the literal reading needs one per enrolled device, unimplementable at the second phone. A hash of a system-minted secret is not a key: it opens nothing. Locked decision 2 is untouched (it governs provider credentials); a minor and not a patch because what a column holds is a decision; §11 stays signed and nothing else moved.** See the doc's change log): the tenancy and work-context model above today's single `shop` row — `organization` → `shop` → `location` (the online store is a location, not a boolean); `app_user` · a closed four-value `role` enum (owner / manager / operator / support_break_glass) · `membership` + `membership_revocation` as an append-only grant/revocation pair on the `retention_hold` precedent, never a mutated row; `device` + `device_credential` + revocation as a second auth principal so a shared counter phone and a personal self-view can coexist (022 P3); `bin` · `batch` + `batch_close` + `batch_source_bin` (A1 — a join table with a composite PK and real FKs, not a jsonb `bin_id` array) carrying 032 §5's source boxes, declared slice mix, written listing floor and pass; `task` + `task_event` with state as a view, never a column, and three nullable subject FKs under `CHECK (num_nonnulls(...) = 1)` rather than a polymorphic `subject_table`/`subject_id` (A2 — 030's A2 correction applied a second time), written as ordinary same-transaction `workflow` inserts with the cross-module effects of a task deferred to E02-B09's outbox (A3); and `labor_shift` + `labor_shift_confirmation` **derived at the location level and never per person**, reporting-owned so the dependency rule itself forbids a scan-flow INSERT, with the paid-hours denominator supplied by the owner once per shift and a **PROPOSED** 45-minute gap / 12-hour ceiling rule whose parameters are stored on every row and re-derived at G3 from Pilot A's gap distribution **segmented by staffing pattern** (A6), with the smaller candidate gap plus a 006 row as the non-bimodal fallback. Uuids throughout — LCIDs name catalog things only (030 §2.5), so 030 I3 is unaffected. Tenant isolation as three layers with owners: the `shop_id` contract here, the T24 runtime per-request assertion at E03-B03, RLS with transaction-local context at E03-B04 (blocked on E02-B07's request transaction, 029 §12), plus the T35 accessor contract — covering the pre-G2 `created_by`/`confirmed_by` strings as well as `operator_id` (A5) — making a per-operator report an architecture-gate failure rather than a review finding, a worked cross-tenant K1, and the go-live posture stated against 019 §3.0 and §5: all three layers are a go-live condition, the E03-B03→E03-B04 interval sits inside G2 on synthetic data only, and if E03-B04 slips the gate slips with it (A7). The representative shop's seed rows and a 25-location chain on the same schema; an expand-only `004` migration sketch with the immutable/config split, what stays nullable and five explicit no-backfill statements (`operator_id`, `batch_id`, `item_slice`, pre-G2 shifts, `created_by`/`confirmed_by`); 14 invariants mapped to future test files; five alternatives (flat shop-only, full org hierarchy, per-location tenants, per-operator shifts, shift-as-view) and why they lose; the three 029 amend-by-a-row entries this record owes but does not make; and the five cannon questions with two answered-and-decided (Q4 → three subject FKs, Q5 → keep `organization` on the consent ground) and three carried forward. Fowler's dissent — defer `organization` — preserved in §11. Every "today" claim REPRODUCED at `737e12d` with file:line or the verbatim grep, re-run after the branch was replayed onto main rather than carried across. Ratification moves nothing up the ladder: §2–§7 stay ASSERTED and nothing is TESTED. **v1.2.0** applies 048 §7.4's amend-by-a-row (`device_credential.key_ref` → `token_hash`). **v1.3.0** applies E03-D09's: §4.3's A4 keeps its guarantee and changes its mechanism — `shop.organization_id` is enforced by a validated `CHECK (organization_id IS NOT NULL)` rather than by `SET NOT NULL`, because 000-docs/044 §2 (ratified after this record) makes that a CONTRACTING statement needing a `-- contract:` header `migrations/019` cannot honestly write. Restricted internal. |



| 036 | [036-AT-DECR-longbox-physical-item-copy-sku-custody-and-duplicate-invariants-2026-09-04.md](036-AT-DECR-longbox-physical-item-copy-sku-custody-and-duplicate-invariants-2026-09-04.md) | Decision record (E02-B05, **RATIFIED 2026-09-04** via a two-lens cannon, v1.1.0 — six amendments absorbed, none declined; see the doc's change log): the physical copy as the inventory identity, distinct from the scan session. `physical_item` is one immutable row per copy (shop-scoped, FK'd to `lcid_registry` by `edition_lcid`, created from a confirmed `human_confirmation` or from bulk intake) with every later fact appended — custody, listing, sale, return, write-off — and current state always a derived view. A code is an append-only **assignment**, not a column (A1). **No `quantity` column**, argued from fungibility: comics fail all three fungibility tests because condition is assessed per copy as a grade range plus a defect array (`001:122-131`), so two copies are two rows forever and a fungible vertical models a pack-owned stock lot instead. **The SKU seam is named** (the gap 030's gate audit flagged): a SKU lives on the commerce-owned `listing_link` mapping (029 §2.7, E10-B03), never on the workflow-owned copy. **Duplicate invariants D1–D5**: at most one active listing per copy across all channels — with the honest negative result that a partial unique index **cannot** express that across two tables, so a `physical_item_active_listing` **materialized index over the log** carries the primary key that closes the concurrency race a check-then-insert trigger leaves open, guarded on contents by I9 and on shape by I18; at most one copy per channel listing, by a real partial unique index; a sale on any channel deactivates the binding in the same transaction, and the in-store and online paths write the same facts through **one** function. Carries the **T18 daily reconciliation as four arms of SQL** that never group by `edition_lcid` (so a shop holding six copies of one book is never a finding), the K1 discriminator (a duplicate is K1 only when it reached a *live* listing), and double-sell repair by appending — both sales stand, a resolution names which is fulfilled, a refund is its own fact, nothing is deleted. Custody as events; holds (widening `retention_hold.target_table` to name a copy); disputes as `task`'s fourth subject FK; write-offs; and consignment provenance under T33 with no seller PII. The sale family is **workflow-owned** (A3): an in-store sale has no connector in it. §7.5 writes the **four 029 amend-by-a-row entries** this bead owns — workflow gains `physical_item`, `identity_resolution` and the sale family; commerce gains `listing_link` and the seam sentence — plus 034 §7's three quoted verbatim so all seven land in one edit. Eighteen invariants with test names; six rejected alternatives; six cannon questions, five answered. Three binding obligations: measure the serializable path before building the index (O-A), land I18 with it (O-B), no sticker UI before the counter is observed (O-C). Every §1 today-claim REPRODUCED at `7359140` with file:line or the verbatim grep and its exit status, re-run after the rebase rather than carried across. Restricted internal. |

| 037 | [037-AT-DECR-longbox-condition-and-grading-policy-2026-09-04.md](037-AT-DECR-longbox-condition-and-grading-policy-2026-09-04.md) | Decision record (E08-B01, v1.1.0, **RATIFIED 2026-09-04** by the acting head after a two-lens cannon — `product-critic` and `legal-advisor`, **both ACCEPT-WITH-CHANGES**, every amendment absorbed and none declined, no v1.0.0 decision reversed; no council, because no 019 threshold moved and no locked decision was reopened — locked decision 5 is *applied* here): the condition and grading policy. **Three statements and one shape** (the v1.1.0 reframing): a **range** is a *format*, not an author; the **shop's opinion** is the human's call and the only condition that reaches a listing; an **estimate** is any machine-authored condition value, internal-only and never rendered, which is where the ratified rule *"the system never proposes a condition"* (035 §2.5, 022 P1) becomes testable; a **certified grade** is a third-party slab attribute quoted with its grader named. **The brief's framing that the system proposes a range is recorded as contradicted** by 022 P1 and 035 §2.5 under 018 §3 precedence, with the shipped pre-filled `VG`/`FN` default (`public/app.js:233-234`) named as the live instance — and the two taps that framing was meant to save are given back by a **single seven-word ladder** (one tap a band, two the range between), routed to E05-B09, with invariant **I3 extended to forbid every suggestion-shaped substitute**: sticky last range, frequency-ordered options, shop-level defaults. **Terminology:** the seven band words stand and **plus/minus are declined** (the published Overstreet mapping shows the half-steps *are* numeric rungs — VF 8.0–8.5, FN/VF 7.0–7.5 — so a modifier is a number with the digits filed off; a shop transacting in "VF+" maps out of the range at listing time via E10-B04, and nothing enters the schema); codes never render, and a range reads **"between Fine and Very Fine"** — never "FN/VF" and, after the cannon, never "Fine to Very Fine", which a trade reader hears as that same numeric rung; the **defect vocabulary is reconciled to nineteen options in three tiers**, adding `tape`, `subscription_crease`, `cut_or_missing_coupon`, `crease_color_breaking`, `trimming_suspected`, `married_copy_suspected`, `signature_unwitnessed` and narrowing `cover_crease` without rewriting history. **The pressing rule was rewritten on a legal BLOCK:** v1.0.0's "the honest policy is silence" conflated *not knowing* with *not saying* — **if the shop knows pressure was applied it goes in the notes**, silence means the shop does not know, an unevidenced suspicion is not called, and a later buyer discovery does not fault a shop that did not know (N7b keeps `pressed` out of the capture vocabulary without keeping it off the listing). **Eight proposed 021 rows (C12–C18 and C5 v2; numbers provisional — 021 is not edited here):** a condition line reading `Condition: between {band} and {band}.` and always closing **"Photos are of the actual book."**, default-on and removable-per-listing on the 022 Q7 / C5 treatment; a not-a-certified-grade line; a slab line that *replaces* it, leads with the label and points at the grader's *current* definitions; the condition-screen copy (helper: *"You're describing the book, not grading it."*); a restoration line opening *"Please look closely before you buy"* and closing *"We decided to tell you."*; a buyer one-liner; a price-source line **routed to E09/E10 and gated on the pricing path being more than a stub**; and **C5 v2, adding "Sold by {shop name}."** on the legal lens's second BLOCK — **the shop is the seller of record and Longbox is a tool vendor** (§5.0), with liability, indemnity, dispute terms and refund-policy representation routed to the E01-B05 counsel checklist. Plus blocklist rows **N1–N8**, where **N8** is the cannon's sharpest catch: no artifact may place a condition value adjacent to a model output, no condition-path identifier may be named `ai`/`auto`/`predicted`/`suggested`/`estimated`/`recommended`, and **no demo of the condition flow is recorded until D1 is fixed** — because the first "AI grading" claim Longbox could make would be a silent one, made by a video. And one retirement: any graded-versus-raw dispute-rate comparison, because **no such published statistic exists**. **Five owner-review triggers**, with the value floor moved onto `shop_pricing_policy` (default $100, a 006 row governs the default; lowering free, raising recorded) and a value-triggered review **not dischargeable by tapping publish** — the owner confirms or edits the condition field, writing a superseding `condition_assessment` (**I10**). **Disputes follow 033 §5.8** with a **one-tap dispute pack** (photos + the condition line *exactly as published*, not a re-render + timestamps) and an **outcome field** on E11-B04's record so K2 and T9 have an input; a good-faith confirmation of a system proposal is a **system defect and never an employee fault**; refund and return terms stay the shop's; **T8 is never evidence in a dispute** (019 T8 internal-only, 021 B5). **The marketing rule is satisfied by removing its antecedent: we do not market grading at all.** **Sourced throughout with its failures recorded:** the Overstreet word-to-number mapping (secondary — Overstreet's own domains never fetched), CGC's five label types and scale (primary), the FTC deception, substantiation and .com-Disclosures standards (primary PDFs), **Midtown Comics using Overstreet definitions "but does not assign numerical grades"** and MyComicShop's public *"inherently subjective process"* — a second independent corroboration of locked decision 5 — plus **the negative finding that neither CGC nor CBCS publishes a grading-is-opinion clause** (only a general warranty disclaimer; the nearest statement is a FAQ), and, added in the cannon round, eBay's **three-business-day seller-response window** and mandatory photo evidence and Shopify's bank-adjudicated chargeback — **which corrected the "~72 h" figure the amendment round supplied**, and for which **no Shopify response-window number could be sourced, so the record states none**. **Ten invariants, all OPEN**, and **five shipped defects opened** (D1 the pre-filled default, D2 codes on screen, D3 codes and no disclaimer on the listing body, D4 the missing C5 provenance sentence, D5 no undo) — the signature says plainly that the policy is ahead of the code. Six alternatives rejected; the five v1.0.0 questions all answered in §11. Restricted internal — **never extract §1, §2, §4 or §5**; only §3's strings are intended to become shop-facing, and only as 021 rows through the T26 pre-send step. |
| 040 | [040-AT-DECR-longbox-workflow-state-machine-and-authorized-transitions-2026-09-04.md](040-AT-DECR-longbox-workflow-state-machine-and-authorized-transitions-2026-09-04.md) | Decision record (E02-B06, v1.1.0, **RATIFIED 2026-09-04** via a two-lens cannon, both ACCEPT-WITH-CHANGES, nine amendments absorbed): the canonical workflow state machine. **Two machines, not one** — the scan-session episode (intake → captured → proposed → confirmed → conditioned → priced → drafted, plus parked/resumed and the owner's reopened) and the copy/listing lifecycle 036 already models, handing over at 036 §4.3's `scan_session_item_resolution` row. **State is DERIVED from the immutable log**: a seven-rung implied ladder witnessed by records that already exist, plus **one** append-only `scan_session_transition` table whose `kind` enum is four values, for the only four things nothing else records. `scan_session.status` — the one mutable column in the scan chain — is deprecated then dropped, and `scan_session` finally joins the `001:174-176` append-only trigger array. Fifteen (from, to) pairs with their driver and witnessing record; nine forbidden transitions (nothing publishes from Longbox; nothing skips `confirmed`; a contradiction blocks one-tap; condition is never machine-driven; state never falls by inference; `drafted` requires a condition) and six guards, the sixth being 037 §4.1's five owner-review triggers. Adds `listing_status_observation` because `shopify_draft` is append-only and 003's `published`/`delisted`/`archived` values therefore have **no legal writer** — which is also where T19's missing `published_by` lands; and `human_confirmation.outcome`, a 019 §3.0 G2 prerequisite 003 never landed. **The nine amendments:** every transition carries a **causal reference** to the record it was issued against, and the derivation compares against that rather than a clock (A1); one transition table over a proposed split, with per-kind terminality guards (A2); **`abandoned` becomes a derived predicate, never a fact row**, so 033 §5.2's "never auto-completed" holds literally (A3); **`failed` is struck** — every failure is already a record (A4); 029 §12's request transaction **does not exist**, so four guards and I14 are blocked and **ratification does not retire the half-written-chain risk** (A5); the plain view ships only with a benchmark fixture and a CI plan-shape gate, mirroring 036's O-A (A6); a negative architecture test fails a publish-surface import at merge (A7); the `SELECT *` status leak becomes an E02-B08 acceptance line (A8); the second device's stale read becomes an E05 acceptance criterion and `request_idempotency` retention defers to E13-B01 (A9). Two dissents preserved. Twenty invariants with test names — four blocked, two written to fail on the current tree; seven rejected alternatives; four 029 amend-by-a-row entries. Every §1 today-claim REPRODUCED at `aa448bb` with file:line or the verbatim grep and its exit status. Restricted internal. |
| 041 | [041-AT-DECR-longbox-append-only-observations-correction-supersession-aggregation-and-purge-2026-09-04.md](041-AT-DECR-longbox-append-only-observations-correction-supersession-aggregation-and-purge-2026-09-04.md) | Decision record (E02-B07, v1.1.4, **RATIFIED 2026-09-04** via a two-lens cannon — `rich-hickey-reviewer` + `martin-kleppmann-reviewer`, both ACCEPT-WITH-CHANGES — plus a `legal-advisor` review of the purge; twelve amendments absorbed, none declined; **both architecture lenses re-executed the by-execution findings on a clean `postgres:16` and reproduced every one verbatim, and no §1 finding is disputed**): how an append-only log stops being merely immutable and becomes *honest*. **The through-line:** a log whose rows cannot be edited but which cannot say who wrote them, in what order, against what, or under whose authority is unfalsifiable rather than truthful — and unfalsifiable is worse, because it cannot be corrected either. Nine decisions: an **observation envelope** applied to the existing witness tables as a specification with a test, not a base table and not an `event(payload jsonb)` — adding `authored_by ∈ {human, system, provider}`, which makes a machine-authored condition **unrepresentable rather than forbidden** (019 T7, 037 §1.1, 040 F4 gain a CHECK instead of a grep), plus `actor_role`, `definition_version`, and `observed_at` only where observed and recorded times differ, with `created_at` declared to *be* `recorded_at` rather than renamed across thirteen tables and **no derivation permitted to order by a client-supplied clock**; **correction and supersession** through one writer with four integrity rules; **029 §12's request transaction** at READ COMMITTED with a constraint where one exists, a `FOR UPDATE` anchor on `scan_session` where one does not, and SERIALIZABLE only for anchorless sweeps — because a lock names the row a guard depends on and an isolation level does not; the **tie-break 040 §3.4 owes**, paid with a per-session `session_seq` assigned under the lock the transaction already takes; a **materialization rule** generalising 036's arbiter into a named form with the **replay drill** as its test; 036 §13's **O-A and O-B**; **lawful purge**, where bytes are destroyed *before* the tombstone is appended (a false statement in an append-only table is unrecoverable; a missing one is a retry) and field-level erasure is made possible without an UPDATE by the rule that **the log holds references, not personal data**; the **append-only-trigger bypass detector** 019 §3.6 files as this bead's directive; and confirmation that 030's `lcid_registry` and `identity_resolution` fit unchanged. **Six of §1's twenty-two today-claims are REPRODUCED by execution, not by reading**, and they are the record's sharpest content: a `human_confirmation` can supersede **itself**, a pair can supersede **each other**, and either leaves the session with zero current confirmations **permanently and unrepairably**; **one shop can supersede another shop's record** — a reproducible cross-tenant write against non-waivable T24; and the append-only guarantee is policed by a test that **passes with every trigger switched off**, because `information_schema.triggers` is blind to `tgenabled` and the default `'O'` yields to a plain `SET session_replication_role`. Twenty-two invariants, four ⛔ blocked on the unwritten transaction helper and six written to **fail on the current tree**; eight rejected alternatives; two parameters signed OPEN rather than guessed; **The cannon changed five things and the changes are the interesting part**: the trigger hardening moves to **migration 0, alone, ahead of the transaction** (it shares no dependency and closes a live bypass of the project's non-negotiable data model, and shipping the transaction first would ship it against an unenforced model); the same-scope **composite FK becomes mandatory**, because a non-waivable tenant boundary may not sit behind a mechanism this record itself proves is bypassable; §4.2 **splits** into a decided preference order and an **OPEN** lock-versus-isolation question, gaining a per-guard lock-coverage table that names **write skew** and finds one guard where the lock's rows and the predicate's rows did not coincide; `session_seq` is pinned as **commit order, not act order**, so a replayed offline write is never read as when the operator acted; and the purge keeps only a **keyed HMAC under a key held outside the database** instead of a raw hash, so retention-for-detection is revocable by destroying a key rather than permanent in a row that can never be edited. Legal added hold kinds, a 24-month bound on purged-row operator context, a minimal purge record that never describes what was deleted, and a routing of every statutory question to counsel **uncited**. One ratified record is amended by exactly one row (040 → v1.2.0). **Nothing is built, nothing is TESTED, and ratification retires neither the three live schema defects nor the half-written-chain gap — all live at `12470b3`, and the first thing that changes any of them is migration 0.** |
| 042 | [042-AT-DECR-longbox-api-event-error-idempotency-and-concurrency-contracts-2026-09-04.md](042-AT-DECR-longbox-api-event-error-idempotency-and-concurrency-contracts-2026-09-04.md) | Decision record (E02-B08, **v1.4.4, RATIFIED 2026-09-04** via a two-lens cannon — `martin-kleppmann-reviewer` + `martin-fowler-reviewer`, both ACCEPT-WITH-CHANGES, both reproducing every §1 claim they sampled; **eleven amendments absorbed, none declined, and one draft decision STRUCK**): the versioned API, its resource shape, the error contract, idempotency, optimistic concurrency and the outbound event contract — **the boundary record, where the guarantees 040 and 041 won in the log are currently given back at the wire.** §1's twenty-four today-claims are REPRODUCED at `b72033f`, each with a `file:line` or the verbatim command and its exit status, and **every negative row names the exact symbols searched** rather than asserting "zero lines" — the 041 v1.1.1 gate-audit lesson applied rather than repeated. **The findings:** there is **no version anywhere** — every `/v1/` in the tree is Anthropic's, eBay's or Shopify's; **no declared request or response shape** — the Zod literals are validators inside handlers, Fastify's `schema` option is used nowhere, and there is no OpenAPI document, so E14-B04 has nothing to generate from; **three incompatible error envelopes**, and the only machine-readable `code` in the system is the 413 the handler *threw* rather than composed, while `error` holds a string on some paths and a Zod `flatten()` blob on others and `POST …/identify` returns its **success body** on a 502 and a provider's exception message verbatim on a 503; **three unrelated conditions share one 409** and the test suite discriminates them by matching English prose; **the only client renders the raw error body to the operator as text** on seven paths, so a `flatten()` blob is operator-facing copy today against 022 P6's *"a screen never blames the person"*; **the identify response carries a raw confidence float, the provider id, the model name and the per-call cost**, with the only guard a static grep over one client file — the contract emits what the principle forbids and the enforcement lives downstream of the thing that emits it; **`llm_rerank`'s id is never returned**, so 040 A1's *required* causal reference is **unsatisfiable on the confirm path** (a missing `RETURNING`, blocking a ratified design, named by no prior record); the `SELECT *` `status` leak 040 A8 files as this bead's acceptance line is a **class, not an instance**, because `getSessionEvents` interpolates seven table names into `SELECT *` and so publishes every column 041 §2 will add on the day its migration applies; **no idempotency key, no correlation id** (three months after 034 §3.1 declared one), **no rate limiting**, and `.dependency-cruiser.cjs` **does not exist**, so every "unrepresentable at merge" argument is conditional on E02-B10. Two live tenancy exposures are **re-reproduced rather than claimed as new** — the unauthenticated shop enumeration (034 E9) and the unscoped photo tree (022 P7; one of 019 §3.0's eight G2 safety controls) — and **one inherited citation is CONTRADICTED**: 040 §7 and this bead's own `Docs:` line attribute the call envelope to "029 §10", which is *Consequences*; the real hooks are 029 §11, 029 §12.3 and 034 §3.1, filed as a 040 patch rather than performed here. **Seven proposed decisions.** A **path** version `/api/v1/` — argued over a header on the ground that this project's evidence regime is citation and a header appears in no grep, no bead note, no Caddy matcher and no test URL — with **additive-only** change inside a version, a two-batch deprecation window, and **Zod in `src/contracts/v1/` as the source of truth with a committed, diff-gated OpenAPI build artifact**, which is 041 §9.2's one-list-many-readers construction applied to the wire; the departure from 030 §5.2's single-rate pack posture is **reconciled explicitly** — same discipline, different cadence, because a pack version is an as-of pointer for stored rows while an API version is a coordination point between running programs. A **response-DTO rule** generalising 040 A8 from the `status` instance to the `SELECT *` class, plus a **route-table classification** feeding the walk 019 T35(b) and 034 §3.3 already require — every route is tenant-scoped or an **allowlisted exception with a reason and a closing bead**. **One error envelope** — `code`, `message`, `details`, `correlation_id`, `retryable` — with a code registry as an enum, and the rule that does the real work: **the server never emits operator prose at all**, so 022 P6, 021 B17/B19 and 022 P8 become properties of a registered-copy table the client reads rather than a grep over one client, and gate outcomes (contradiction, terminal session, missing condition, hold) become codes instead of sentences. **`Idempotency-Key` on every mutating route**, where the `UNIQUE (shop_id, key)` constraint does the serialising and there is **no in-flight state at all** — the three-state machine turned out to be a mutable status column one record after 040 removed exactly that from `scan_session` — with the key **minted when the operator acts, not at replay**, which is 041 A4's rule about `session_seq` applied to the key. **Optimistic concurrency** on 040 A1's causal reference, in the **body** because `request_hash` covers the body and a header would let a replay carry a different world-view than the original; the record fixes the wire shape only and hands the screen to E05 (040 A9). An **event contract of reference-plus-envelope, never values** — because a payload is a second source of truth, is un-purgeable, and ages while the row it copied is corrected — with **names generated from 041 §9.2's declared table list**, so an event name cannot drift from the schema and three of 029 §2's aspirational names turn out to be facts nothing records. And a **per-shop, never per-IP** rate posture whose metered class **fails closed to the manual path rather than to an error** (019 K4 applied to a throttle we impose on ourselves), with both numbers signed **OPEN** because no request-rate figure exists anywhere in the ratified set. Twenty invariants naming test files that do not exist — **three ⛔ blocked** on the transaction helper, **four ⚠ conditional** on an architecture gate that is not installed, and **seven written to fail on the current tree**; ten rejected alternatives incl. RFC 9457 `problem+json` (thin margin, §13 Q3); nine questions, **and the cannon answered three of them against the draft**. **A2 STRUCK the generated event catalogue**: a naming scheme fixed against zero running code — no outbox, no consumer, no publish call — is **unfalsifiable**, and the draft had already met a result the scheme could not accommodate and reported it as a feature; §7 keeps only what a client or E02-B09 can be tested against, and `event_version` is struck in favour of the referenced row's `definition_version`. **A3 overruled the record's own posture on numbers**: 018 A3 caps claims of *fact*, not safety floors, so the two rate parameters and 041's `tx_max_retries` take **PROVISIONAL, explicitly non-evidentiary circuit-breaker defaults** derived from 035's item ceiling — refusing to set any number was not humility but an unprotected shop — while **`abandon_after` stays OPEN** because it describes what a session reads rather than protecting the system. **A8 ruled the allowlist as drafted WAS a waiver by declaration** — *a declared exposure against a non-waivable threshold is not the same epistemic object as a declared trigger exemption* — so rows carry `kind ∈ {exemption, defect}`, the shop picker is a **defect**, and **no defect row may exist at G2**. **A5 and A6 found §5.3 "proved only in English"**: the construction holds only on one connection held for the request's lifetime and only under one fixed lock order, both now invariants **I21/I22** and **E02-D04 acceptance lines**. Plus A9 (a retry that changes `against` is a new act needing a new key), A1 (the reversibility test §2.2 was applying unnamed), A4, A7, A10 (§2.4 validated; "two orders of magnitude" reworded as a direction, not a measurement) and A11. **Four dissents preserved, each resolved rather than overruled; Fowler adopted §4.3 and §3.3/I5 without change, recording the latter as the model the rest of the record should have followed — find the mechanism, not the occurrence.** Twenty-two invariants; **040 → v1.2.1 and 041 → v1.1.2 patched in the same commit**. **v1.1.1 is a post-gate-audit patch — statements of fact only, §14 stays signed:** E6 and E7 were **false at the merge base** by the same mechanism that broke 041 E8 — a substring collision, this time with PR #54's `request_idempotency` pending-list entry, turning "zero lines, exit 1" into three matches and "six lines" into ten — so both are re-derived with their real output and both now **name the symbols that must return nothing** rather than resting on a count; the findings survive and E7's is stronger, since the three new matches are this record's own §5.2 exemption declared but not built (`CREATE TABLE` grep, exit 1). Plus I20's "sixteen" `db.query` calls corrected to **eight**, the ⚠-conditional count from four to **three** and the blocked count from three to **five** (A5/A6 added I21 and I22 and neither tally was re-counted), E11's `:153` → `:152`, I2 aligned with Q4's three grounds, and §0/§9.1 recording that **E02-D05 shipped as migration `006`**, leaving E02-D04 the only unshipped precondition. **v1.3.0 (E03-D09)** adds ONE stated exemption class to §5.1: an authentication act, whose only effect is a `Set-Cookie`, requires the `Idempotency-Key` header (048 §5.2's second CSRF mechanism) and takes no `request_idempotency` row — because §5.3 stores a response BODY, and a replayed `201` with no cookie is a phone that believes it is signed in and holds nothing. Restricted internal. |
| 043 | [043-AT-DECR-longbox-transactional-outbox-saga-job-queue-retry-dead-letter-and-replay-boundaries-2026-09-04.md](043-AT-DECR-longbox-transactional-outbox-saga-job-queue-retry-dead-letter-and-replay-boundaries-2026-09-04.md) | Decision record (E02-B09, **v1.1.0, RATIFIED 2026-09-04** via a two-lens cannon — `martin-kleppmann-reviewer` + `rich-hickey-reviewer`, both ACCEPT-WITH-CHANGES, **twelve amendments absorbed, none declined, one draft decision STRUCK and one draft mechanism REPLACED by an invariant**): how an effect that leaves Longbox is made replay-safe. **The outbox is two append-only tables in the same database** — `outbox` (intent: reference + 041 envelope + payload hash, never values) and `outbox_attempt` (every attempt, including a `started` row so a crash leaves a trace) — written inside the request transaction, with **no mutable status column and no exemption**, because the lease is *derived from the attempt log* rather than stored (terminal / in-flight / due are three predicates, so a stale claim needs no reaper) — **and the claim and its `started` row commit in ONE transaction on ONE connection (A1), the one place the record draws an interleaving diagram**, under a named clock assumption (A7). The draft's `payload_hash` is **STRUCK (A6)**: the unique constraint already made its case unconstructible. **The event catalogue 042 A2 struck and assigned here is written**: nine names under `longbox.<module>.<verb>`, derived from five source tables by keeping only appends a consumer outside the writing module could act on, with every exclusion citing the rule that excludes it, and `definition_version` as the event's only version. **The Shopify draft becomes a job**, made idempotent not by a retry token — Shopify's Admin GraphQL offers none — but by `productSet`'s `identifier: { customId }` **upsert**, verified against the API reference; guarded so an upsert can never set a listing a human published back to DRAFT — **and the guard FAILS CLOSED (A3): zero observations plus an existing draft is a refusal, not a proceed, so E02-D07 no longer needs E10-B05 to be safe, only to be useful**; with **no compensating delete** (a DRAFT harms nobody under T19, and deleting the shop's data is the worse act) and orphan reconciliation through the E10-B05 watcher — closing the E02-D04 obligation that a Shopify DRAFT can exist with no `shopify_draft` row. Retry is exponential with jitter, permanent `userErrors` are dead-lettered on the first attempt, the ceiling is a **PROVISIONAL non-evidentiary floor** under 042 A3, dead-lettering is a terminal attempt kind rather than a DLQ table, and **nothing re-drives without a human record**. Replay boundaries: delivery always, the Shopify call only under the key and the guard, a witness row never, `cost_log` never. One poller per process on `FOR UPDATE … SKIP LOCKED` with a fixed lock order, so two processes are safe by construction. Consumer idempotency is gated **at the registry** by a parametric test over every registered consumer plus a lint (A2); **T22 is scoped to database recovery and the post-restore reconciliation lag is owed, not invented** (A4); guard-refusal dead letters report distinctly from T17 (A5); and the one behavioural dependency neither lens could reproduce — that Shopify's `customId` upsert is immediately consistent after a create — is an **assumption signed OPEN, closed by a dev-store measurement rather than a citation** (A11). Twenty invariants (one ⛔ blocked on the unwritten `app_user` table, one ⚠ on E10-B05's watcher, two ⚠ on E02-B10's architecture gate, four failing on the tree at `17b98ab`), nine rejected alternatives, four 029 amend-by-a-row entries, three 019 amend-by-row candidates filed for E00-B03 and not applied, eight questions answered in §13, and four dissents preserved verbatim in §14. Implementing bead **E02-D07** with six acceptance lines. |
| 044 | [044-OD-STND-longbox-migration-discipline-expand-contract-rollback-and-architecture-gate-2026-09-04.md](044-OD-STND-longbox-migration-discipline-expand-contract-rollback-and-architecture-gate-2026-09-04.md) | Operating standard (E02-B10, v1.0.0) — **a STANDARD, not a decision record**: every rule it carries is already ratified in 029 / 040 / 041 / 042 and is cited rather than re-argued. **Expand/contract as a checked rule:** four contracting statement shapes (`DROP TABLE`, `DROP COLUMN`, `ALTER COLUMN … TYPE`, `SET NOT NULL`) fail `pnpm migrate` unless the file declares a `-- contract:` header naming the expand migration it retires and the 006 decision-log row that signed it; the lint runs before any connection opens, so `--dry-run` reports it too. **The ledger gains a checksum** — added by the runner, not by a migration, since the ledger is the runner's own bookkeeping — so an edited applied file fails loudly instead of printing `skip`; a row applied before the column existed adopts its digest once, loudly, which is the path every real database takes exactly once. **Prior-snapshot upgrade tests:** checked-in `pg_dump` fixtures at `003` and `006` are restored and migrated forward, asserting the append-only trigger set is back at `'A'` (the E02-D05 DROP-then-CREATE downgrade hazard), that the grant step succeeded (proved from the app connection, not the log line), that 041 §10.1's `authored_by` derivations landed on pre-existing rows, and that `session_seq` is still NULL because it is never backfilled. **Compatible rollback:** a rollback is a NEW expand migration that reverses the visible effect — never a down-migration, never a row deletion (036 D5), and the contract half is not reversible at all because a dropped column's values are gone (040 §8.3). **The architecture gate, in two halves:** 029 §3.3's dependency-cruiser config with N1 (the `$1` backreference) and N2 (routes → platform db) fixed as that record demands, plus **N3 found here** — the specified `^pg$` never matches a resolved npm path, so the rule was green by construction until a negative fixture caught it; and `pnpm arch`, the non-graph half carrying 042 I5's no-`SELECT *`, 042 I22's lock-order lint (vacuous today by design), 040 §8.2's writer inventory and the `cost_log` single-writer rule. Every allowlist row carries 042 A8's `kind ∈ {exemption, defect}` with an exact count and a closing bead; **four defect rows stand at G2** — three this gate declares plus 042 §3.4's `GET /api/v1/shops`, which no gate here can see — **and their closing beads are the OPEN E02-D08/E02-D09, not the closed decision beads E02-B07/E02-B08.** §8's branch-protection change is APPLIED: `Architecture gate` is a required check and `main` now carries 8 contexts. Restricted internal. |
| 045 | [045-AT-DIAG-longbox-photo-to-listing-workflow-diagram-2026-09-04.md](045-AT-DIAG-longbox-photo-to-listing-workflow-diagram-2026-09-04.md) | Workflow diagram: the photo-to-listing swimlane in ASCII + Mermaid, grounded route by route, rendered at `.arch/generate-2026-09-04T10-52.html` and published at demos.intentsolutions.io/longbox/. |
| 046 | [046-AT-DECR-longbox-asset-inventory-data-flow-trust-boundaries-and-threat-model-2026-09-04.md](046-AT-DECR-longbox-asset-inventory-data-flow-trust-boundaries-and-threat-model-2026-09-04.md) | Decision record (E03-B01, **v1.1.0, RATIFIED** 2026-09-04 after a two-lens cannon — `security-auditor` + `martin-kleppmann-reviewer`, both ACCEPT-WITH-CHANGES, eleven amendments absorbed and none declined): the security perimeter, mapped once and in one place. Twenty-three asset classes with owner, 014 §10 class, a `file:line` home, a retention answer and an eight-principal read/write matrix; **thirteen numbered trust boundaries** drawn in 045's lanes (phone↔server, the two database roles, disk, the public mount as a boundary in its own right, the BYOK provider hop, PriceCharting, eBay, Shopify outbound, the inbound webhook that does not exist, CI↔secrets, backups); a STRIDE pass that reports the control existing **today with its `file:line`** or the word *none*; **seventeen abuse cases** — the careless or malicious employee, the second shop reading another's data, the stolen phone, key-ref confusion, prompt injection through a cover, replayed keys, outbox replay, trigger bypass, a restore resurrecting purged photographs, webhook forgery, log leakage — each with a control-today / control-decided / 019-detector triple; and a **ranked gap list (G-1…G-27)** that corrects the gate order: **G2 comes before Pilot A, not after it**. Two findings belong to no prior record — `shop_credentials.key_ref` + `base_url` is a credential-exfiltration primitive held shut only by the absence of a write surface, and `confirm.issue` is unbounded client JSON reaching a storefront's product HTML unescaped. Finds 019 T31's scope exceeds the CI secret scan by five surfaces and one cadence, and retires one 022 statement of fact (the `%` copy defect is gone at `2242547`). **At ratification:** E26's negative claim is CORRECTED — `.gitleaks.toml` has existed at the repository root since `253d5bc`, and the draft's evidence was an aliased `ls | grep` rather than the symbol; E14 and E15 are joined, because the storefront-HTML injection is authored by the PHOTOGRAPH and posted verbatim by the one-tap path, so A16 is re-scored high/high; the contradiction gate is found null-gated over a model-supplied confidence, so `{nulls, confidence 0.99}` reaches one-tap today; a thirteenth boundary (**B13**, the provider RESPONSE) is added; `pnpm fixture:schema` runs `pg_dump` with no `--schema-only` inside the repository (**A23/A17**); STRIDE cells are sub-labelled structural / mechanism / absence-noted; the `key_ref` control moves from the column to `resolveKeyRef`'s own body; and the public uploads mount is closed by **deletion**. Six discovered beads filed (E03-D01–D05, E06-D01), seventeen invariants, thirteen rank-1 gaps each annotated with its size. Restricted internal — names live exposures. |
| 047 | [047-AT-DECR-longbox-lcid-namespace-lifecycle-merges-splits-aliases-and-identity-invariants-2026-09-04.md](047-AT-DECR-longbox-lcid-namespace-lifecycle-merges-splits-aliases-and-identity-invariants-2026-09-04.md) | Decision record (E04-B01, v1.1.1 — the E04-B01 gate-audit patch re-anchored every `030:NNN` cite to 030 v1.2.1, repaired four non-030 cites, re-sourced §0's percentage rule to `021:62`, and pointed §12.4's owner column at E04-D01/E04-D02; no decision changed, **RATIFIED 2026-09-04** via a two-lens cannon — `rich-hickey-reviewer` + `martin-kleppmann-reviewer`, twelve amendments A1–A12 absorbed, none declined, all nine questions answered): the Longbox Catalog ID itself. An LCID names a `collectible_definition` or an `edition` and nothing else — never a copy, a session, a condition or a slab; the string is `lb.<kind>.<vertical>.<payload><check>`, an opaque CSPRNG payload behind a prefix that may encode **only what can never be corrected**, with no timestamp and no sort order; the registry INSERT **is** the mint, insert-only, never speculative, never reused; there is **no status column and no PROPOSED LCID** — the lifecycle is appended merge, split and retirement facts; a merge names its survivor and rewrites nothing, and resolution splits into `resolve` (follows merges) and `asStated` (the trail, which never resolves); a **split defaults to AMBIGUOUS plus a review obligation** rather than a silent continuation; external identifiers are four classified alias namespaces in which a grader cert is a **copy** fact and never an edition alias; `identity_resolution` stays a fact per (confirmation, corpus version), and `edition_signature` stays **distinct from `identityKey`** because merging them would move a 019 T1/T3 measurement rule. **v1.1.0 absorbs twelve cannon amendments:** the mint path is **deliberately AP on a signature collision** with no UNIQUE on `edition_signature` (A1, new I17); `resolve` reads a **materialized survivor projection maintained inside the merge's own transaction**, with a benchmark fixture and an index-only CI plan gate (A2, new I18) and a **transactional, never-TTL** cache-invalidation obligation on E06-B02; `resolve` is **TOTAL over five outcomes** including `NOT_YET_MINTED` (A3); `asOf` is **MANDATORY** at the module boundary with `resolveCurrent()` at named call sites (A11); **019 T1 scores on `asStated`** (A7, an E07-B01 obligation); a crosswalk certification write is a **catalog-authoring act** needing a role detector T24 cannot supply (A4, on E04-B06); grader and `cert_number` are **copy facts in every vertical**, amending 030 to v1.2.0 by a row (A6); the bounded walk **raises at write time and returns an outcome at read time** (A10). Eighteen invariants, none blocked on missing machinery and all blocked on 030's unbuilt tables. Dissents preserved verbatim in §14. |
| 048 | [048-AT-DECR-longbox-identity-sessions-mfa-invitations-and-recovery-2026-09-04.md](048-AT-DECR-longbox-identity-sessions-mfa-invitations-and-recovery-2026-09-04.md) | Decision record (E03-B02, **v1.5.0, RATIFIED 2026-09-04** via a two-lens cannon — `security-auditor` + `martin-kleppmann-reviewer`, both ACCEPT-WITH-CHANGES; **twenty amendments absorbed, none declined, eight dissents preserved verbatim, all nine questions ruled in §13.0, and every amendment mechanism rather than decision** — §14 records that both lenses affirmed the construction itself without change): user identity, sessions, MFA, CSRF, invitations and recovery, on a phone three people share. **Three §0/§1 evidence defects repaired before any amendment, by the record's own discipline** — E10 rested a negative claim on a *count* from a command returning 46 lines (22 codes at `07972fa`, 23 at `cf9c289`) and now names the symbols that must be absent; E2 printed a ten-line command beside a two-line claim (the `Request` substring collision that broke 041 E8 and 042 E6/E7, a third time) and now prints `addHook\("onRequest"\|preHandler`; E3/E12's `public/app.js` cites were restated at `cf9c289` after PR #64 moved four of the six files §1 cites, with the unmoved `src` cites re-run rather than carried — the security lens's D6 is the dissent about exactly that and stands as method. **The amendments, all mechanism:** two cookies rather than one (one forces the device session to share the operator session's lifetime and signs the phone out of the shop on every idle-out); `chain_id` so liveness is constant in rotation depth and revocation is one row; the session read as `SELECT … FOR NO KEY UPDATE` inside the request transaction `runIdempotent` already opens, at a stated position in 042 §5.3(b)'s lock order; a grace window on the rotation race, because the draft made a double-tap at the counter sign the phone out — *"the security control silently manufacturing the outage it exists to prevent"* — with reuse revoking the operator chain and never the device chain; the membership write locking every live session first, which answers Q6 **yes, a lock on the hot path**, stated as a cost; cross-chain liveness as a formula with `shop_id`/`location_id` denormalized at issuance; 041 R1's mandatory composite FK on `(rotated_from, app_user_id, device_id)`; the **reuse-detection overclaim struck** — it fires only when the legitimate client later rotates, so idle expiry is the load-bearing control; lockout as a **growing delay that never closes a door**, with a per-device ceiling, the DoS trade stated, count-then-verify under `FOR UPDATE` on `operator_pin` and the window's `now()` as the third sanctioned exception under 041 §2.6; the PIN fixed at six digits with a denylist and a process-environment pepper; the insider residual verbatim plus a RULE forbidding any artifact or 021 C-row from calling operator attribution non-repudiable; a fourth CSRF mechanism this record owns (`Sec-Fetch-Site`) after Q4's *"three properties that each fail loudly is two properties and an absence"* was upheld, with a dedicated registrable domain filed to E13; `Idempotency-Key` enforced ahead of the multipart parser and the session read; no CORS plugin asserted over `package.json`; a separate AUTH allowlist with a redirect-only row for the 308 aliases; membership-first execution so a wrong tenant *costs* the same as an absent one; rate classes for the four sessionless routes; invitations redeemable only on a device enrolled to the same shop (settling Q8 on the right axis — not shop size); `auth_attempt` **failures only**, the draft's justification for successes having been false since T35(c) reconciles from `app_session`; AEAD with per-row nonce, row id as AAD and `key_version` day one; `last_used_step` by conditional UPDATE; and a recovery code substituting for the second factor only. **§11 grew to seventeen invariants**; **four implementing beads filed** — E03-D09 (the substrate, the work the acceptance line was silently carrying), E03-D06, E03-D07, E03-D08; **034 amended to v1.2.0** in the same commit. The v1.0.0 design, unchanged by ratification: Three principals and 034's four roles unchanged (the bead's three-role framing corrected — `manager` is in the schema and not in the pilot). **Two sessions, not one:** a long-lived device session pinning shop and location, and a short PIN-derived operator session on top of it — both append-only issuance facts with **no status column**, liveness derived, and idle expiry enforced by **rotation rather than a mutable `last_seen_at`**, which makes a spent token evidence that the cookie was copied. **Staff hold a PIN that is worthless off an enrolled device and never a password**; a low-entropy secret is defensible only as a knowledge factor on a possession-bound channel. TOTP required for `owner`/`manager`/`support_break_glass`, **passkeys deferred on the recovery ground rather than the security one** (a lost TOTP secret is a procedure this record can define; a lost passkey is a platform we do not control), with the authenticator key's rule split from its custody. **No synchronizer CSRF token** — `SameSite=Strict` on a `__Host-` cookie, the already-mandatory `Idempotency-Key` header and the total absence of CORS, each asserted rather than remembered, because a token that is issued and never compared fails silently where all three of these fail loudly. The **session becomes the only source of `shop_id`**, a wrong tenant answers `SHOP_NOT_FOUND` (a distinct forbidden code is an existence oracle over the table the shop picker enumerates today), and `GET /api/v1/shops` becomes *my shops* — the design that **retires 046's last `defect`-kind allowlist row**, and, by ordering the auth hook before the rate hook, closes G-20 inside this bead rather than E13-B02's. Invitations and device enrollment are single-use by UNIQUE constraint and **delivered out of band by the owner**, because this repository has no mail or SMS transport and inventing one would put an inbox at the root of a shop's catalog. Recovery is code-first, with the residual case on the existing audited break-glass runbook; security questions, SMS, magic links and a silent super-admin are refused by name. **(v1.5.0 rules that the codes go to all THREE MFA-required roles — `owner`, `manager` and `support_break_glass` — because §8's "owner-only" contrasts with STAFF, whose PIN reset is in person under §8.3, and a manager who loses a phone has the same outage as an owner.)** Lockout is a derivation over append-only `auth_attempt` facts, not a per-process `Map`. An unnumbered four-migration list and a bead split. **Applies one amend-by-a-row on 034 §2.8** (`device_credential.key_ref` → `token_hash`) now that §14 is signed, with the residual — bearer material in a browser cookie jar — stated rather than left implicit and a keypair end state filed to E03-B05. **Nothing is built at ratification: §1 REPRODUCED at `cf9c289`, everything else ASSERTED, all seventeen invariants name test files that do not exist and all are blocked on 034 §4.1's unwritten tenancy migration, and ratification authenticates no request.** **v1.2.0 (E03-D09's invariant review)** adds one stated residual to §9.3: a pair inside its lockout delay is refused before any hash is computed, so the body withholds *"this pair has recent failures"* and the clock does not — hashing while blocked is refused as a denial-of-service surface, and the leak is bounded by the lockout itself. **The substrate §2–§9 designs is BUILT by E03-D09** (migrations 019/020, `src/services/auth/`), which is where seventeen invariants stop naming files that do not exist. **v1.2.0** adds one stated residual to §9.3 (the lockout delay's timing, from E03-D09's invariant review). **v1.3.0 NARROWS §6.4 (E03-D08's invariant review, acting head):** *my shops* returns the one shop the SESSION PINS rather than every shop the caller holds a membership at — §3.5 pins shop and location on the device session, so any other shop already answers `SHOP_NOT_FOUND` (§6.5) and listing one would both offer an entry that 404s and disclose the name of another shop that person works at to whoever holds this shop's counter phone (022 P3, 019 T35). **The consequence is stated as a PRODUCT decision, not a display narrowing: an operator with memberships at two shops cannot switch shops from the picker — switching shops is a device re-enrollment (§7.3), and the roaming-owner case is not served by v0 and is E03-D07's to reconsider.** §6.4 also stops describing the two branches as one query: a device-only session holds no membership, so its shop is read by the session's own enrollment id; only the operator branch is membership-rooted, which is where R13 binds. **v1.4.0 (E03-D07)** rules §7's three open choices from the bead that built them. **v1.5.0 (E03-D06)** absorbs what building the SECOND FACTOR settled — AEAD with a per-row nonce, the row id as AAD and a `key_version` whose rotation is additive; the replay guard's conditional UPDATE; the lockout anchor MIRRORED onto `user_authenticator` rather than extended from `operator_pin`; one per-person budget across TOTP and recovery codes; the recovery-code parameters as PROVISIONAL floors; and the nomination as a table whose ask is refused only when SILENT — **and records the finding that makes it consequential: the FIRST factor (`user_credential`) is §10.1's M3 remainder that §10.2's table assigned to NO BEAD, and §3.1's two session kinds are both device-bound, so the second factor ships with no route, reached from CLIs, with §12.4's new row 3a handing the first factor and the privileged-session SHAPE to E03-D11 (`longbox-e5b.3.21`).** |
| 049 | [049-AT-DECR-longbox-card-identity-schema-normalized-signature-and-vertical-pack-registry-2026-09-04.md](049-AT-DECR-longbox-card-identity-schema-normalized-signature-and-vertical-pack-registry-2026-09-04.md) | Decision record (E04-B03, **v1.0.0, RATIFIED 2026-09-04**): the versioned CARD identity schema, its normalized signature, and the vertical-pack registry. Six decisions — **two verticals (`sports-card`, `tcg-card`) sharing one pack module** (030 A5's single-rate pack version makes one `card` pack a split that 047 I1 later forbids); the edition signature **`set + number + variant + parallel + language`**, which **amends 030 §5.4 to v1.3.0 by a row** because the sketch left `parallel` out of the key and signed a Silver Prizm #12 identically to a base #12; **no default language, ever**; **`printRun` an edition fact and `serialNumber` a copy fact**, extending 047 A6's denylist and moving it ABOVE every pack into `copyFacts.ts`; **the registry leaves the pack** into `src/catalog/packRegistry.ts`, because a second vertical registered in the old layout is an import cycle `no-circular` refuses; and **`claimIsUsable` as a sixth pack function**, closing a comic branch that was wearing field names in `identityResolution.ts:198` and would have skipped every card claim silently. 030 §6 rule 1 becomes architecture-gate **rule 8**. Unusually for a DEC record, most of it is **TESTED**, not asserted — §7 maps each invariant to its test. **Registering a vertical is not launching one: E19-B06 still gates any second vertical, and no card pack row, corpus, provider or shop exists.** |
| 050 | [050-AT-DECR-longbox-secret-vaulting-byok-rotation-deletion-and-spend-ownership-2026-09-04.md](050-AT-DECR-longbox-secret-vaulting-byok-rotation-deletion-and-spend-ownership-2026-09-04.md) | Decision record (E03-B05, **v1.0.5** (E03-D18 patch 2026-09-05: the dead `003:134-143` citation in §4 now points at 036:588/:614) — **v1.0.4, **RATIFIED 2026-09-04** — the gate audit of `f5e8fd9` returned NOT-READY with twelve patches, all applied at v1.0.2 and READY at `afb445d`; the code half landed as PR #74 (`92b2c25`) and §14 was signed at v1.0.3 and none reversing a ruling; the verdict stands until re-run** — the RECORD HALF only; the code half is a follow-on commit on the same bead, and the identity substrate it cites as its actor model lands under E03-B02): secret, BYOK and service-account custody, rotation, deletion and spend ownership. Six questions to the two-lens cannon (§2) with both lenses' positions and the acting head's ruling preserved. **§3 custody** — the environment variable per `key_ref` is the process-visible form, a SOPS file decrypted by an `ExecStartPre=` step into a mandatory mode-0600 root-owned `EnvironmentFile` **on tmpfs** (`/run/intentsolutions/longbox.env` under a `RuntimeDirectory=`, or `/dev/shm`; never under `/etc` or `/var`, per the estate standard's decrypt-to-disk anti-pattern and because `/run` is outside the borg set by construction) is its only legitimate source, the process never decrypts, and the encrypted DB column is REJECTED on three grounds (locked decision 2; an envelope key living in the same process defends against nobody in 046 §2.3's matrix; and a column value lands in a 30-day Object-Lock backup a revocation cannot reach). **§4 rotation** — two append-only tables, `shop_credential_version` (introduction) and `shop_credential_retirement` (a separate fact, because `retired_at` would be an UPDATE the trigger refuses), liveness as a PREDICATE with no status column, the newest live version winning, an overlap window bounded by a PROVISIONAL floor and reported rather than auto-retired, and `cost_log` recording the `credential_version_id` as a REFERENCE (041 §8.4) under the enforced single-writer rule. **§5 deletion** — "unreachable" defined per storage form, three steps (SOPS edit, **restart, which re-derives the tmpfs file from the SOPS source**, retirement row), and the receipt that names what was made unreachable and never claims the provider-side revocation Longbox cannot perform. **§6 spend** — `spend_owner` NOT NULL and DERIVED, never declared; a shop with no credential may use the service account as a **pilot affordance with an expiry**, attributed `longbox`; per-owner ceilings as PROVISIONAL floors in 042 §8.4's class (500 shop-owned / 150 service-account paid identify calls per shop per day, non-evidentiary); at the ceiling the system degrades to the MANUAL PATH and never to a stub answer. **§7** the key never enters a Longbox request path — the deploy path for v1, the one-time write endpoint deferred and pre-constrained (write-only, no read-back, no echo, never a column/log/prompt/`cost_log`). **§8** closes 046 G-25, A15, G-9's E03-B05 half and §9.2's obligation, and names G-16, G-13, G-14 and A14's prose half as NOT closed with their owners. **§9** eleven invariants with their test files; **§10** three expand migrations by name only; **§13** nine things deliberately not decided, two of them NEEDS COUNSEL. Restricted internal. *(Number 050 was taken because E04-B03 concurrently claimed 049.)* **v1.0.4 (2026-09-05, E13-D01): §13 item 10 ANSWERED — the VPS's own runner was read and `/run`/`/dev/shm` are under none of its source roots, so §2 Q1 ground (iv) is measured on the deploy host rather than inherited (intent-os PR #576, squash SHA `77000dd3`). No ruling moves.** |
| 051 | [051-AT-DECR-longbox-vertical-pack-manifest-certification-and-registration-2026-09-04.md](051-AT-DECR-longbox-vertical-pack-manifest-certification-and-registration-2026-09-04.md) | Decision record (E04-B04, **v1.0.7, **RATIFIED 2026-09-04** (PR #73 `6203b35`) — the `longbox-invariant-reviewer` returned PASS-WITH-NOTES on `4ace390` and the `longbox-gate-auditor` returned **NOT-READY** on v1.0.0; v1.0.1 applies every finding and the verdict stands until re-run. **Restricted internal**, raised from Internal because §4.1/§8 enumerate grading mechanics 021 B5 limits): the vertical-pack manifest as shipped code, its certification, and how a pack becomes a row. Six rulings — the manifest ships **eleven fields, each with a named consumer** under 030 A3 / Fowler F1, with `normalizedSignature`, `providerCrosswalk[].normalize`, `capabilities`, `listingTemplate` and `evalSlices` omitted and the reason given for each; **`certify()` is a pure gate over twenty-seven codes**, chief among them 030 §5.4's second half — **a manifest declaring `grader`, `cert_number` or any copy fact as an identity field FAILS, which moves 049's C10 from ASSERTED to TESTED (C11 untouched)**; the authoritative vertical codes **`cmc` / `spc` / `tcg`**, discharging 049 §8; **one manifest per registered vertical** (implementing the standing F3 ruling at `049:310`) and **`packRegistry.ts` does not collapse yet** (F2); **registration is an operator script under `MIGRATE_DATABASE_URL` that REFUSES the application role by a check, not a migration and not a boot seed** — a pack is DATA, the integration suite asserts the migration ledger is unchanged by three registrations, and `assertSchemaOwnerOrThrow` closes the hole v1.0.0 left open (`resolveMigrateUrl` falls back to `DATABASE_URL` outside production and the app role has INSERT); and the **card ladder drops MINT and GEM** on 021 B21, which post-dates 030 §5.4's sketch and takes **030 to v1.3.1** by a patch that annotates the sketch rather than editing it. The signature positions a manifest declares are **PROVED against the signature function by perturbation**, in four cases; the claim that this would have caught 030 §5.4's pre-v1.3.0 `parallel` defect is a **counterfactual covered by shape**, not a run, and v1.0.1 says so. **Registering a vertical is not launching one: E19-B06 still gates any second vertical, and no card corpus, provider or shop exists.** |
| 052 | [052-AT-DECR-longbox-comic-identity-schema-and-normalized-signature-2026-09-04.md](052-AT-DECR-longbox-comic-identity-schema-and-normalized-signature-2026-09-04.md) | Decision record (E04-D03 `longbox-e5b.4.15`, recording **E04-B02**; **v1.0.2, **RATIFIED 2026-09-04** — the gate audit of `d217225` returned NOT-READY with two blocking evidence defects and seven smaller repairs, all applied at v1.0.1 and none reversing a decision; the verdict stands until re-run**): the versioned COMIC identity schema, its normalized signature, and 047 A8's static guard — the decisions behind PR #69 (`f6adbc4`) lifted out of a 006 row and a PR body into the place a decision lives. Two Zod schemas on 030 §5.3's split (work `{series, publisher, volume, year}`; printing `{issue, variant, printing, cover, coverDate, distribution}`), `COMIC_IDENTITY_SCHEMA_VERSION` as the third and distinct version number, and the ratified four-field signature `series + issue + variant + printing` with `distribution` and `coverDate` held OUT of it because widening a ratified signature is an amendment and not a convenience. **Five decisions, each with the alternative it rejected:** the signature is computed by a **SERVICE, never a trigger** (a trigger needs a second copy of the normalisation in PL/pgSQL while `normalization_version` versions exactly one rule, cannot resolve a per-pack function registered as data, and forecloses 030 A4's multi-signature aliases); **unknown is distinct from absent in `attributes` and DELIBERATELY COLLAPSED in the signature** (an absent key means no statement was made, an explicit `null` is a statement of "not known", and a dedupe key must not distinguish two records describing one book with different degrees of silence); **the signature spans BOTH schemas and the edition READS its parent's `series`** rather than copying it, because a corrected series would otherwise leave stale copies indistinguishable from deliberate aliases under append-only rules; **catalog supersession is an UNMADE decision, not an omission** — the architecture gate's rule 6 caught a `supersedes_id` write and an exemption row would have been the wrong fix, since `supersede()` scope-checks a shop, a session and a `session_seq` and a catalog row has none of the three (E04-B11); and **every per-pack function is REGISTERED and fails closed, never called by name** — the invariant review's WARN 1, where the comic composer ran BEFORE the fail-closed signature function, so a second vertical would have been comic-signed with nothing refusing. **047 A8 discharged in both halves:** a dependency-cruiser edge rule, `architectureRules.ts` rule 7 (no shared import, no `FIELDS`/`FIELD_LIST` constant, failing loudly on a missing subject), and `checkIdentityPairEdit`, which fails a diff editing `identityKey` beside any catalog identity file without a 000-docs/006 row — **on a PR and on a push to `main`**, because `main` takes direct paperwork commits and an owner bypass. Thirteen invariants, twelve TESTED and one ASSERTED by absence — and at v1.0.1 that one states its evidence honestly: the grep for `supersedes_id` in `src/catalog/` returns **three** lines, not none, of which two are comments and one is an as-of READ predicate, so the property is that **no INSERT or UPDATE names the column** and the burden is 040 §8.2's writer inventory rather than a grep count. §8 records what E04-B03 moved afterwards (the registry, the copy-fact denylist, `claimIsUsable`) and §9 says plainly that **no cannon was run** — the authority is the merged PR, the 006 row and the PASS-WITH-NOTES invariant review, quoted. Restricted internal. *(051 was concurrently claimed by E04-B04 and is not yet on `main`.)* |
| 054 | [054-AT-DECR-longbox-least-privilege-rbac-permission-matrix-and-actor-audit-2026-09-04.md](054-AT-DECR-longbox-least-privilege-rbac-permission-matrix-and-actor-audit-2026-09-04.md) | Decision record (E03-B03 `longbox-e5b.3.3`, **v1.1.2, RATIFIED** 2026-09-05 at close, PR #88 `9571ef1`; the parenthetical that follows is the PROPOSED-era description — **v1.1.1, PROPOSED** — the two-lens cannon WAS dispatched (`security-auditor` + `martin-kleppmann-reviewer`, both ACCEPT-WITH-CHANGES, **fifteen findings folded, none declined, three dissents preserved verbatim in §14**) and the acting head signs at CLOSE, after the gate audit): least-privilege RBAC, the permission matrix, and the actor audit. Four decisions. **(A)** Who may do what is ONE versioned constant — eleven permissions with a scope and a plain-English explanation in the contract layer, `ROLE_GRANTS` in the identity module, split so that adding a route can only NAME a permission while widening one is an edit a reviewer looks for; the route table gains a `requires` column enforced at ONE site (the auth hook's sixth step, inside the tenant branch, after the membership read, proved by OFFSET rather than by substring on the evidence standard `rate-class-enforcement` had to learn), and a tenant-prefixed route declaring no permission is REFUSED. The three shop roles hold the nine scan-flow permissions identically — 031's representative shop is one owner and two operators at one counter — and differ only in who may bring a person or a phone into the shop; **`support_break_glass` holds NOTHING**, so 022 P7's "no invisible super-admin" is an empty list rather than a promise. **(B)** The actor audit is a DECISION record, not an activity log: `authorization_decision` names the MEMBERSHIP GRANT and never the person, the route TEMPLATE and never the URL, carries `matrix_version` (without it, answering "under what authority" means re-running today's rules over yesterday's grants and calling the answer history), and refuses eleven named columns by test — **every refusal recorded, an allowance only where the route MUTATES**, because an allowance on a read joined through the membership is a record of what a named person looked at (022 P3). Written OUTSIDE the request transaction, so a decision can outlive a rolled-back request and not the reverse — correct for an audit of AUTHORITY, wrong for one of ACTS, which the domain rows already are; fail-closed for an allowance and best-effort for a refusal; ONE writer under a new `pnpm arch` rule. **(C)** Nobody grants above their own rank — closing a LIVE escalation E03-D07 shipped: `issueInvitation` checked MEMBERSHIP and not ROLE, so an operator could invite an OWNER, while the route table claimed the service "checks the role"; the refusal is renamed `not_a_member` → `not_permitted` and both causes share one name so nobody can walk the ladder. **(D)** Break-glass is bounded in three places that fail differently (CHECK, query, decision) plus a new `NOT VALID` CHECK for 034 I5's third clause — *never granted by its own holder* — which had no mechanism; 019 T35(c)'s reconciliation ships as a query with its scheduler left to E13-B04. **Eighteen invariants, all TESTED.** ⚠ **The cannon changed four things that had shipped:** the audit's `session_chain_id` INDEX is dropped (no reader; the migration comment claiming it served T35(c) was FALSE, and what it bought was a fast "everything this person did on this phone that day" — the covert timeclock 022 P3 forbids, inside a table whose record argued it was not one); the recording rule was **already failing** — `issueInvitation` and `issueEnrollmentCode` are the two acts that change who may act at a shop, are reached from scripts, and so recorded NOTHING under a rule keyed on whether a ROUTE mutates — and widens to *mutating OR privileged* with both CLI paths writing decisions; the claim *"one request is one decision"* is **WITHDRAWN** (an `Idempotency-Key` replay re-enters the hook before `request_idempotency` dedupes, so the ratio is N:1 — accepted, documented, pinned by a test, E03-D15 owns the fix); and the audit INSERT ran before any rate bucket, so the shop's `ordinary` token now comes first. Also: a revocation is read as an early expiry in the reconciliation; every row carries the deploying COMMIT beside the semver; `authored_by` is dropped; the never-self-granted CHECK is stated as a SHAPE constraint whose real bound is that no code path mints the role. Closes 046 G-2's RBAC half and G-11's G2 exit condition; **does NOT build the `identity` accessor module** (which the record now says must also gate THIS table), so 019 T35(b) stays OPEN — **with an owner at last, E03-D17 `longbox-e5b.3.27`**. Files E03-D14/D15/D16/D17 and hands 022 P3's monitoring notice to E01-B06. Migration **028**, numbered past a gap because E03-B06 holds 026/027 on the same base. Restricted internal.
| 055 | [055-AT-DECR-longbox-key-ring-escrow-or-accepted-ring-loss-2026-09-04.md](055-AT-DECR-longbox-key-ring-escrow-or-accepted-ring-loss-2026-09-04.md) | Decision record (E03-D12 `longbox-e5b.3.22`; **v1.0.4, **RATIFIED 2026-09-04** (gate re-audit READY at `bcb00d8`) — the cannon was **DISPATCHED**, `security-auditor` and `joe-armstrong-reviewer` both returned **ACCEPT-WITH-CHANGES**, and of the **seventeen findings** returned (F1–F13 with two CRITICAL and four HIGH, A1–A4) **sixteen required change, all applied, none declined, F13 clean**; **v1.0.2 applies eleven gate-audit items — statements of fact and pointers only, no ruling reversed, every substantive claim verified, including re-pinning PR #87 from `16f17ef`, a commit reachable from no ref, to `843652c`**; both dissents preserved verbatim in §14). **Escrow the loss-bearing secrets, or accept that losing them ends every sign-in and every connector.** The bead asks about two AEAD rings; the record decides **three secrets** — `LONGBOX_PIN_PEPPER`, `LONGBOX_AUTHENTICATOR_KEY_V<n>` and `LONGBOX_CONNECTOR_KEY_V<n>` (053, PROPOSED, PR #87) — because they share one SOPS file, one `ExecStartPre` and one host. The finding that decides it: the documented fallbacks compose badly — recovery codes are hashed with the pepper **by inheritance rather than by decision**, the in-person PIN reset needs a factor under the ring, and 048 §8.2's break-glass is held by a role whose own factor is under the same ring. **RULING: Option A (escrow), adapting the estate buzz precedent and NOT copying it** — the recipient list inverts, because Longbox runs on the host buzz was allowed to include. **v1.0.1's two critical corrections are what make it citable:** RULING 3 is **narrowed** — excluding the deploy host key removes the **direct** reach only, and a five-hop chain from committed configuration (VPS root → host age key → `secrets.prod.sops.yaml` → the dev-box borg passphrase → the replica rsynced onto the VPS nightly → `~/.config` inside it) reaches the estate key anyway, so **nothing may call the escrow "separated"**; and filing under the ordinary `secrets.*.sops.yaml` name would match a **generic `path_regex`** encrypting the escrow to the estate key, the VPS host key **and an unattributed third recipient the estate books as a P0** — so the escrow needs its **own explicit block**, and its exclusion is a **precondition**. **Option B is the honest INTERIM and now has a boundary**: no shop beyond Pilot A enrols an operator or completes a connector install until the escrow exists and its drill has passed once. Acceptance is an **open**, never a decrypt, against a **purpose-minted drill row** and never a real operator's second factor. **Eleven contracts** with the test each will get — including **I9**, the recipient set as a tested fingerprint list rather than prose, which is the security dissent's central charge, and **I10**, a key-liveness canary because nothing today catches a variable holding the *wrong* value. Thirteen open items, a **§6.1 last-resort stub** whose no-escrow branch restores nothing, both residuals stated, all cadence figures PROVISIONAL and declared non-evidentiary, **no statute cited**. **Nothing is built; no invariant is tested.** **v1.0.4 (2026-09-05, E13-D01): four statements of fact amended by appended sentences — §6.1's last-resort runbook now exists in intent-os, E9's unmerged-branch caveat is retired, I9's BLOCK half landed and its FILE half did not, and §5.2's candidate fix is prescribed IN TEST AND ON NO HOST. No ruling moves.** |
| 053 | [053-AT-DECR-longbox-connector-oauth-least-scopes-token-lifecycle-consent-and-revocation-2026-09-04.md](053-AT-DECR-longbox-connector-oauth-least-scopes-token-lifecycle-consent-and-revocation-2026-09-04.md) | Decision record (E03-B06 `longbox-e5b.3.6`, **v1.0.6, **RATIFIED 2026-09-04** (PR #87 `f4a03b6`) — both lenses were DISPATCHED and returned ACCEPT-WITH-CHANGES; the ruling STANDS, every finding folded in at v1.0.1, the gate audit's statement-of-fact repairs at v1.0.2, the invariant re-verification's one note at v1.0.3, and at v1.0.4 the re-pin of §1 from a commit the rebase left on NO BRANCH to its merged equivalent `ec9b465`, every command re-run and identical (no decision moved, no re-cannon). Both dissents preserved verbatim in §14.1**; nothing in it may be cited as settled until §16 is signed after the gate audit and the invariant review): connector OAuth, least scopes, the token's lifecycle, consent and revocation. Closes 046 **G-14** (*"static Shopify admin token; no OAuth lifecycle, no revocation"*), the defect 050 §8 handed here by name. **§3 custody, and it is the one ruling a reader should check first.** The connector access token is AEAD ciphertext in a column, which 050 §2 Q1 REFUSES for a BYOK key — and that ruling stands unchanged, because the distinction is not "connector versus BYOK" but **two predicates a future credential can be tested against**: (i) the value is MINTED BY THE MACHINE, with no human in its custody path, so the SOPS→tmpfs→environment form cannot carry it without somebody reading a bearer credential off a screen; and (ii) it is REVOCABLE AT ITS ISSUER without a Longbox act **and the issuer says so**, which defuses 050's decisive Object-Lock ground — a ciphertext in a 30-day immutable backup is a ciphertext of a credential already dead at the provider. A BYOK key satisfies NEITHER; a TOTP secret satisfies (i) and is 048 §4.2's precedent for the same envelope. The envelope is 048 R18's clause for clause under its **own** ring (`LONGBOX_CONNECTOR_KEY_V<n>`, boot-required), because one key for two subsystems means one compromise is two. **The residual is stated and Lens S's narrowing is preserved**: the process holds the ring and the ciphertext, so this defends a stolen DATABASE DUMP IN ISOLATION — **not** a backup and **not** a compromised process. v1.0.0 claimed the backup and had not shown it: the estate's borg include set carries `/etc` (the age key) and the database dump in the same archive, so E13-D01 `longbox-e5b.13.11` must separate them before the wider sentence is true (046 **A18**). **§4/§7 the lifecycle** — 050 §4's model one connector over: two append-only facts, liveness as a predicate, newest live version wins, and **a shop with versions and none live is REFUSED rather than falling back to the static token**, which is the half that makes an uninstall real. **§5 five append-only tables** (`migrations/026`), no exemption and no mutable column, every guard a UNIQUE index; the webhook receipt's `shop_id` is nullable **alone in the file**, with the reason recorded — a `shop/redact` for an already-offboarded store must be recordable, and inventing a tenant would be worse than a null resolution. **§6 the scopes** — `write_products` and `read_products`, each argued (the read is required by 043 A3's fail-closed guard, not by convenience), pinned by a test, and checked two-sidedly with the write⇒read implication applied one way; **the strongest entry is an ABSENCE** — no `write_publications`, because 019 T19's non-waivable zero is better enforced by not holding the capability than by hardcoding a field. **§8 authenticity** — an anchored store-domain check that is an SSRF control (the next thing an install does with `shop` is POST this app's client secret to it), timing-safe HMACs over the query and over the RAW body, both verified before any row, and a duplicated query parameter refused outright rather than resolved first-or-last. **Two ratified invariants are amended rather than dodged, in the same PR: 042 → v1.4.3** (§5.1 gains a SECOND exemption class — an inbound provider callback, whose caller cannot send the header at all, with both mechanisms REPLACED by stronger ones and each member NAMING the UNIQUE index a contract test reads out of `migrations/`) and **048 → v1.5.2** (I6(d) restated as *no GET mutates on the strength of an AMBIENT CREDENTIAL*, its own §5.3 ground, with the carve-out a declared kind asserted as set equality both ways). 021 gains **§4a**, a candidate-row state, holding the install-screen and end-of-authority copy this bead refuses to settle. **At v1.0.1 three things changed that a reader should not have to diff for.** **(S1)** the claim that this defends *"a stolen backup"* is WITHDRAWN and narrowed to **a stolen database dump IN ISOLATION** — the estate's borg include set carries `/etc` (the age key) and the database dump in the same archive, so E13-D01 `longbox-e5b.13.11` must separate them before the wider sentence is true; 050 §2 Q3's counterweight (a BYOK key is in every SOPS revision and backup too, so the column loses nothing on backup exposure and gains a destruction primitive) is recorded and is NOT used as a rebuttal. **(S2)** a receipt sentence was FALSE — *"only the merchant can end it there"* — and §7.3a replaces it with an app-initiated revoke, two columns recording what was CALLED and what came BACK, and four rendered outcomes including *no attempt*; the endpoint's existence at the pinned API version is signed **OPEN** in 043 A11's idiom, and a wrong guess degrades to a truthful receipt rather than a false one. **(H1)** §2 Q1's test gained its fourth cell — **(i)-true/(ii)-false**, machine-minted and silently revocable, which is most vendor OAuth — and states the third implicit predicate: **the credential's death must be OBSERVABLE to this system**. §3.0 adds the clause that admits the pilot's own human-path Dev-Dashboard token to the same lifecycle; §3.0a cites 046's new **A18**, the offline-database-copy adversary that justified the column and was in nobody's matrix. Twelve invariants (§11), eight alternatives with their grounds (§13), twelve things deliberately not decided (§15) of which the data-subject questions go to counsel. Restricted internal. **v1.0.6 (2026-09-05, E13-D01): §14 item 2 amended — the archive separation is REPRODUCED IN TEST at intent-os PR #576, squash SHA `77000dd3` and is applied on NO host, so the scope claim stays *a stolen database dump in isolation* until a deploy is recorded in 006.** |
| 056 | [056-AT-DECR-longbox-row-level-security-and-transaction-local-tenant-context-2026-09-05.md](056-AT-DECR-longbox-row-level-security-and-transaction-local-tenant-context-2026-09-05.md) | Decision record (E03-B04 `longbox-e5b.3.4`, **v1.1.2, RATIFIED** 2026-09-05 at close, PR #90 `a451de8`; the parenthetical that follows is the PROPOSED-era description — **v1.1.1, PROPOSED** — the two-lens cannon **WAS dispatched**: `security-auditor` and `martin-kleppmann-reviewer` both ACCEPT-WITH-CHANGES against a live cluster, §2 carries their VERBATIM statements and the builder's in-band drafts are superseded; the invariant review's BLOCK is fixed, the gate audit's one ruling applied and its re-audit's six fact repairs folded as v1.1.1; the acting head signs at close with the merge SHA): PostgreSQL row-level security, and the third of 034 §3.2's three tenancy layers — the one that survives a handler bug. **§3** the tenant is a TRANSACTION-LOCAL setting issued in the same round trip as `BEGIN` (`set_config(…, is_local => true)`), taken from the session and never from a body or a URL, read by a `STABLE` `current_shop_id()` that returns NULL when unset — so a path that forgets its context sees NOTHING and writes nothing; `SET ROLE` per request and application-side filtering are both rejected, with reasons. **§4** `ENABLE` and deliberately not `FORCE`: the schema owner bypasses (which is why every operator CLI, migration data statement and fixture still works) and the APPLICATION role — which owns nothing, is not a superuser and has no `BYPASSRLS` — is subject to every policy, with a boot assertion that refuses to bind a port unless **the SET of policies that exists equals the set this design emits**, by table, name, command and both predicates; a per-table check was blind to a permissive policy ADDED beside the declared one, and six negative fixtures prove the replacement can fail. `shop` is policied on its own `id`, which makes creating a shop a schema-owner act enforced by the database. **§5** the cross-tenant statements are a CLOSED union of SEVEN named scopes, each carrying its own justification, and the tables they reach carry a `FOR SELECT` policy keyed on **the scopes that table declares** plus, on three tables, a `FOR INSERT` policy whose check names the ROW — v1.0.0's scope-agnostic `FOR ALL` boolean was a cross-tenant WRITE grant and the lens used it; `pnpm arch` holds the call-site count per scope at an EXACT number and the entry count at their sum; the outbox drains one shop at a time. **§6** the two findings this bead produced — a `NOT EXISTS` guard beside a service-scoped table fails OPEN, and an UPDATE that matches no row SUCCEEDS — plus the cannon's rule that a `UNIQUE` over a client-choosable value must be tenant-prefixed. **§7** every table with no tenant is a DECLARED exemption with a reason, and a live table in neither class stops the migrate run. **§8** every view is `security_invoker`, without which a `*_current` read model would have returned every shop's rows. **§9** what it costs, including why the eighteen new indexes are not built `CONCURRENTLY`. **§11** the residuals, of which three may never be softened: R1, the application role can set the context to any value it likes, so this defends a FORGOTTEN PREDICATE and not a compromised process; R4, the cross-tenant audit query (built here by the acting head's ruling — `pnpm audit:cross-tenant`) detects rows whose tenancy is internally inconsistent and is not proof that no crossing occurred; and R7, a foreign-key check runs with RLS OFF, so the app role can attach a row in its own tenant to another tenant's parent. **§13** the alternatives, including the one this version REVERSES. |
