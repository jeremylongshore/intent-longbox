# Decision Record — The Loss-Bearing Secrets: Escrow Them, or Record Ring Loss as an Accepted Availability Risk

**Version:** 1.0.4
**Status:** **RATIFIED 2026-09-04 (v1.0.3)** by the acting head under Jeremy's standing delegation — the cannon in §3 was DISPATCHED to `security-auditor` and `joe-armstrong-reviewer` by the parent session (both ACCEPT-WITH-CHANGES; seventeen returned, sixteen applied); `longbox-gate-auditor` NOT-READY at `5670525` → READY at `bcb00d8` (fast-forwarded onto main). Option A (escrow of all three secrets, adapting the buzz precedent with the deploy host excluded) is the ruling; Option B is the LIVE interim posture until §8's contracts land; nothing here may be quoted as a recoverability property (021 B16) until I4's drill has passed. **AMENDED at v1.0.4 (2026-09-05, E13-D01): four statements of fact, by APPENDED sentences with every original left verbatim — §6.1's runbook now exists, E9's unmerged-branch caveat is retired, I9's block half landed and its file half did not, and §5.2's candidate fix is prescribed IN TEST AND ON NO HOST. No ruling moves and RULING 3's narrowing is untouched.**
**Bead:** E03-D12 `longbox-e5b.3.22` — *Escrow the connector key ring or record ring loss as an accepted availability risk* (epic LBOX-E03 `longbox-e5b.3`, gate G2)
**Filed:** 2026-09-04 · **Author:** `longbox-security-tenancy-builder` · **Owner:** parent session (acting head of board)
**Sensitivity:** Restricted internal (014 §10). It names every loss-bearing secret this system holds, the variables that carry them, the hosts they are rendered on, the recipients an escrow would be encrypted to, and a reachability chain between two estate hosts. **It contains no secret value of any kind and never will**, and it reproduces no age recipient in full — recipients are named by their first characters and by the file that already carries them, per 016 §0's point-at-never-copy rule.
**Scope of this commit:** **DOCS ONLY.** No `src/`, `migrations/`, `scripts/`, `tests/*.ts`, `public/` or `.github/` change lands with it. §8's contracts are obligations on named beads, and every one of them is untested today.
**Governed by:** 018 (evidence), 019 T22/T35, 021 B16, 022 P3/P7/P8, 041 §8.3, 042 §8.4, 046 §8.1, 048, 050, 053 (PROPOSED, PR #87).

---

## Change log

**Version convention** (006 `:6`): a **minor** bump means the content of a decision changed; a **patch** means a statement of fact was repaired with no decision changing. **v1.0.1 is a PATCH under 050's own pre-ratification precedent** — 050 took v1.0.1 and v1.0.2 for acting-head amendments and gate-audit patches *while §14 was still unsigned*, on the rule that **no ruling is reversed**. None is reversed here: RULING 3 is **narrowed**, RULING 4 and RULING 6 **gain boundaries**, and everything else is an addition. A record that is not yet signed is being corrected, not amended.

| Version | Date | What changed | Authority |
| --- | --- | --- | --- |
| **1.0.4** | **2026-09-05** | **Patch — four statements of fact, from E13-D01 `longbox-e5b.13.11`. No ruling moves, no recipient changes, no contract is added or removed, and the ratified text is AMENDED BY APPENDED SENTENCES with every original left verbatim.** **(1) §6.1's stub now has its runbook.** It said *"the full runbook is E13-D01's"*; that runbook exists at `intent-os ops/longbox/RUNBOOK-ring-loss.md` — who acts, the escrow branch, the rebuild, the calls, in 022 P3/P8's language with no statute cited and no recovery implied — and §6.1 points at it. The stub's own answers are unchanged and still true. **(2) E9's harshest caveat is RETIRED.** That row said the VPS include set was documented as living on an UNMERGED branch, so it was *"not even on a merged branch"*. It is now read from `intent-os ops/backup/borg-backup.sh` on `main`, the runner itself rather than a description of one; the E12–E15 class caveat (committed configuration, NOT observed on the host) still applies and is restated rather than dropped. **(3) I9's BLOCK half has landed and its FILE half has not.** The `.sops.yaml` creation rule, the committed allowlist and the test are in `intent-os ops/longbox/`; what is tested is that the BLOCK's declared recipients are a SUBSET of the allowlist and disjoint from its deny list, with `age1csyj…` and `age197narf…` both provably absent. Set equality against the escrow's own SOPS metadata is still owed and cannot be run until the file exists — which §5.2's precondition still forbids. **(4) §5.2's chain row: the cheapest candidate fix is now PRESCRIBED, and the chain is still OPEN.** That row declined to prescribe excluding `~/.config/sops` from `backup-system.sh`; PR #576 does exactly that, with a preflight that refuses the run when the exclusion is missing. **It is REPRODUCED IN TEST AND APPLIED ON NO HOST**, so the last hop is closed in the repository and open on the box, and the four hops upstream of it were never this bead's to close. **Also discharged, and recorded here rather than by editing §9: item 7's registration** — `KEY-CUSTODY.md` gains rows 14–16 for `LONGBOX_PIN_PEPPER`, `LONGBOX_AUTHENTICATOR_KEY_V<n>` and `LONGBOX_CONNECTOR_KEY_V<n>`, names only, each marked NO ESCROW — owed, in the estate register and not in a competing Longbox-local inventory. Evidence: intent-os PR #576, squash SHA E13D01_SQUASH_SHA. | acting head, on E13-D01 |
| **1.0.1** | **2026-09-04** | **The cannon was DISPATCHED and both lenses returned ACCEPT-WITH-CHANGES; seventeen findings returned, sixteen requiring change, all applied, none declined — F13 was returned clean.** ⚠ **The consequential one is F1, and it is why §10 was not signable as v1.0.0 stood.** RULING 3 claimed that excluding the `intentsolutions` host key separates the escrow from the deploy host. It removes the **direct** path only: the security lens traced a five-hop chain, entirely from committed configuration, by which VPS root reaches the estate key anyway — so v1.0.0's sentence asserted a separation the estate's own backup fabric does not provide. **RULING 3 is narrowed to "direct reach removed; transitive reach is E13-D01's jointly with the estate spine fabric"**, and §5.2 carries the chain as a row marked *reproduced from committed configuration, NOT observed on the host*. **F2 is the second critical**: filing the escrow under intent-os's ordinary `secrets.*.sops.yaml` name matches a **generic `path_regex`** that encrypts to the estate key, the VPS host key **and an unattributed third recipient** the estate's own custody register carries as a P0 — so the escrow would have been created readable by the exact host it excludes plus a holder nobody can name. §5.1 now requires its **own explicit `path_regex` block** in the buzz block's form, §5.2 answers that recipient **NO** and makes it a **PRECONDITION**, and §8 **I9** makes the recipient set a tested list rather than prose. **F3** re-homes §9 item 1: the second recipient is not a fresh Longbox question but an existing estate obligation (`KEY-CUSTODY.md` row 13, `spine-u1a.2.3`), and §5.3 now states that until row 10 is confirmed **by measurement**, Option A's availability benefit is **unproven**. **F4** rewrites the drill so it opens a **purpose-minted drill row** and never a real operator's second factor. **F5** gives RULING 4 a cohort boundary and RULING 6 a second disclosure point. **F6, F7, F9, F10, F11** and **A3, A4** land in §8; **F8** takes a position on the recovery-code/pepper coupling; **F12, A1, A2** land in §9 and the new **§6.1**. **§14 preserves both dissents verbatim.** No ruling is reversed and nothing built changes, because nothing is built. | dispatched cannon → acting head, §10 |
| **1.0.3** | **2026-09-04** | **Ratified.** Status and §10 signed after the gate re-audit returned READY at `bcb00d8`; no ruling, invariant, floor or dissent changed. | acting head |
| **1.0.2** | **2026-09-04** | **Gate audit of `ec4a516` (pre-rebase `5670525`) returned NOT-READY on statements of fact and pointers only — no ruling reversed, every substantive claim verified, dissent handling judged correct. All eleven items applied.** **The one that matters most is a citation nobody could resolve:** §0 and E1 pinned PR #87 at `16f17ef`, which is reachable from **no ref**; re-pinned to `843652c`, the branch head, which gives identical output. **Counts corrected**: *seventeen applied* → **seventeen returned, sixteen requiring change, all applied, none declined, F13 clean**; *three HIGH* → **four HIGH**. **Pointers corrected**: the 019 amendment block is **§9.4** (§9.3 is E04-D04's A8 row) and §8 I6 now says the row text is **considered and DECLINED**, not *supplied*, with §9 item 14 routing the question to E00-B03; E7 quotes `offsite-backup.config.json:107` **verbatim**; §5.1 cites the buzz block's form at `.sops.yaml:32-41`; E12 hop (iv) gains `IMPLEMENTATION-MAP.md:111` (`repokey-blake2` — the key is in the repo, so the passphrase alone opens it) and hop (v) gains `:114`'s independent corroboration; **E9 gains the third-class caveat it was missing** — its include set is documented as living on an UNMERGED branch. **Owners named where §8 promised one**: I11 → **E05-D02 `longbox-e5b.5.12`**, I8 → **E11-B06** with 021 the gate (050 §6.5's split); RULING 4 names **`longbox-adk.7` / `longbox-adk.8`** as the beads its cohort boundary binds; RULING 6's charter half is filed as a **038 §6 fill-in row** naming **039** as its shop-facing home (037's precedent); §9 item 9 states the **recorded dependency edge** rather than implying it; §9 item 4a labels *~50 bits* **PROVISIONAL** per `codes.ts:129-131`. **And §7 now records that the bead's own acceptance clause is discharged by §6 rather than by editing 053, with the reason.** No ruling is reversed, no invariant is added or removed, no floor moves, and §10 is still unsigned. **The gate RE-audit of `a5dafcb` then verified every blocker and item closed and returned three one-line repairs, applied here at NO further version bump because they are that audit's own last cells rather than a new revision**: §10 named v1.0.1 where it must name the version it ratifies; **the alias `E05-D01` was already held by `longbox-e5b.5.11`** (the closed UI-percentages bug, and it is cited under that alias in 006, 015, 016 and 000-INDEX), so I11's owner is **E05-D02 `longbox-e5b.5.12`**; and this row cited `5670525`, which the rebase made unreachable — **the same defect this row exists to record, committed inside the correction for it**, now `ec4a516` with the pre-rebase SHA kept beside it. | acting head, on the gate audit |
| 1.0.0 | 2026-09-04 | Initial record. One ruling with a sequence: **escrow is adopted (Option A) for all THREE loss-bearing secrets rather than for the two rings the bead names**, because they share one custody path and therefore one loss event; **the buzz precedent is adapted and not copied**, because its recipient rule inverts for an application that runs on the shared VPS; and **Option B's posture is the honest INTERIM** until the escrow exists, recorded as an accepted availability risk with E01-B05 informed. Eight contracts (§8), seven open items (§9). Nothing is built. | §3's cannon → acting head, §10 |

---

## 0. Evidence posture

018 governs. Every claim about the tree in §2 is **REPRODUCED** — a command and its output, at a named commit — and every claim about behaviour is marked TESTED or ASSERTED. **Every cadence figure in this record is PROVISIONAL in 042 §8.4's class and is explicitly NON-EVIDENTIARY**: it supports no claim of fact, is never quoted as recoverability, availability, durability or a security property at any class (021 B16), and moves when somebody measures something rather than when somebody prefers a different number. **No statute is cited anywhere in this record**, deliberately — the question of what any external obligation requires of key custody is routed to the E01-B05 counsel batch in §9, not answered here.

**Two commits are read, and they are different trees.** `main` at **`dca5da0`** is the tree §2 was reproduced against. **`main` has since moved to `0f41fec`, and §2 is NOT re-run for it**, because all three intervening commits are beads-only (`d1a6e53` filing E03-D14/D15/D16 and E13-D02; `e14cd18` filing this record's own §8 I11 owner; `0f41fec` renaming that bead to **E05-D02** to clear the alias collision the gate re-audit caught) and none touches a file, migration or document §2 cites — stating that is cheaper and more honest than re-printing eleven commands to produce identical output, and it is the discipline 052 §0 asks for when merges land under a record. `feat/e03-b06-connector-oauth` at **`843652c`** is PR #87, which is where the connector ring and doc 053 live and which has not merged. **RE-PINNED at v1.0.2**: v1.0.0 and v1.0.1 cited `16f17ef`, which is reachable from **no ref** (`git branch -a --contains 16f17ef` → empty) — a commit nobody can resolve is not a citation, and it is the same defect class 048's E10 and 042's E6 were caught on. `843652c` is the branch head and gives the identical output. **053 is cited throughout as "053 (PROPOSED, PR #87)"** and nothing here treats it as settled; if PR #87 changes §3.2 or §14, this record's E5 row and §8 I2 are the two places to re-read.

**A THIRD class of evidence enters at v1.0.1, and it is labelled everywhere it appears.** F1's reachability chain and F2's recipient finding are **reproduced from committed configuration in the `intent-os` working tree, NOT observed on the hosts they describe.** Nobody ran `borg extract` against a replica, nobody read a private key, and nobody decrypted a SOPS file to check. Each hop is a line in a file that is committed and readable; whether the fabric behaves as its configuration says is **unobserved**, and every row carrying this class says so in its own cell. **A record that quietly upgraded "the config says" to "we checked" would be committing the defect 013 catalogues, on the one question where being wrong is most expensive.**

**Doc number.** `000-docs/` on `main` at **`0f41fec`** ends at **052** (re-read at v1.0.2; all three intervening commits filed or renamed beads, not docs). **053** is claimed on `feat/e03-b06-connector-oauth`; **054** is claimed by **E03-B03** on its own unmerged branch. This record takes **055** and says which tree the gap was read against, because 052 §0's lesson stands: a register whose numbers are allocated on branches has to name the tree or the next reader files a collision.

---

## 1. The question, stated so it cannot be answered by half

The bead asks whether to escrow **the connector key ring**. That question cannot be answered honestly at the size it is asked, for a reason §2 reproduces rather than asserts: **this system holds three secrets whose loss is unrecoverable from inside the system, they are destined for one SOPS file rendered onto one host, and the worst of the three is not a ring.** Answering for one and leaving the others to a later bead would repeat the exact failure E5 records — an obligation handed to a bead that closed without discharging it, discovered a week later by somebody reading an `.env.example` comment.

So the question this record answers is: **for each secret whose loss ends a capability the system cannot rebuild from its own database, is there an escrowed copy, where does it live, who can open it, and how is that proven?**

---

## 2. What exists today, REPRODUCED

**Rows E1–E11 are reproduced in this repository at the two commits §0 names.** Rows **E12–E15** are the v1.0.1 additions and are reproduced **from committed configuration in the `intent-os` working tree, NOT observed on the hosts they describe** (§0).

| # | Finding | Command and output |
| --- | --- | --- |
| **E1** | **There are THREE loss-bearing secrets, not two.** `LONGBOX_PIN_PEPPER` (048 §9.2), `LONGBOX_AUTHENTICATOR_KEY_V<n>` (048 §4.2) and `LONGBOX_CONNECTOR_KEY_V<n>` (053 §3, PROPOSED). The first two are on `main`; the third is on PR #87. | `grep -n 'LONGBOX_PIN_PEPPER\|LONGBOX_AUTHENTICATOR_KEY' .env.example` → `230,243,245,262,268,274` at `dca5da0`; `git show 843652c:.env.example \| grep -n CONNECTOR_KEY` → `153,154` |
| **E2** | **Two of the three already refuse to boot when absent, in one place.** `loadConfig` calls `requirePinPepper()` and `requireAuthenticatorKey()` unconditionally. | `src/config.ts:243`, `src/config.ts:247` at `dca5da0` |
| **E3** | **The SOPS file that 050 §3 calls "the only copy of record" DOES NOT EXIST in this repository.** There is no `.sops.yaml`, no `.env.sops`, no encrypted secrets file of any kind. 050 §3's custody ruling is a **rule that has not been deployed**, and an escrow of a file that does not exist is a forward obligation rather than a copy. | `git ls-files \| grep -c -i sops` → `0` at `dca5da0` |
| **E4** | **A recovery code is hashed with the PEPPER, and by INHERITANCE rather than by decision** (F8). `hashRecoveryCode` is a one-line delegation to `hashWithPepper` — the PIN helper — and `verifyRecoveryCode` delegates to `verifyPin`. So 048 §8's recovery codes are not independent of the pepper: they die with it, and no record ever decided that they should. | `src/services/auth/secrets.ts:162-163` → `export async function hashRecoveryCode(code: string, pepper: string) { return hashWithPepper(code, pepper); }`; `:167-169` for the verify half |
| **E5** | **The pepper's backup obligation is ORPHANED.** 048 §12.4 row 3 hands *"the PIN/password pepper's custody, backup and rotation"* to **E03-D06 → E03-B05**. E03-B05's record is **050**, which is RATIFIED and closed, and **050 does not contain the word "pepper" once**. `.env.example:241-242` still points a reader at that discharged hand-off. | `grep -c -i pepper 000-docs/050-…md` → `0`; `.env.example:241-242` |
| **E6** | **The break-glass path is under the ring it is supposed to rescue.** 048 §8.2's residual path is a `support_break_glass` membership; 048 §4.2 requires TOTP for `owner`, `manager` **and** `support_break_glass`; every TOTP secret in the system is a row in one `user_authenticator` table sealed under one ring, regardless of role. A total ring loss therefore disables the operator who is supposed to perform the recovery. | 048 §4.2, 048 §8.2, `src/services/auth/aead.ts:104` |
| **E7** | **The estate's escrow precedent exists, is proven, and carries an explicit recipient RULE.** The buzz borg repokey and passphrase are escrowed to a SOPS file in the ops tree. **The rule, quoted verbatim from the configuration that carries it:** *"Recipients: estate key + shared-VPS host key ONLY — NOT the dedicated buzz host key, since escrow readable only from the host it insures is no escrow."* A restore drill destroys the key in a repo COPY and recovers from escrow alone. | `intent-os ops/backup/offsite-backup.config.json:107` (the quoted sentence, verbatim), `:199`; `intent-os ops/buzz/scripts/escrow-restore-drill.sh` |
| **E8** | **That escrow file has exactly two recipients, and one of them is the `intentsolutions` host key.** Safe for buzz — buzz runs on a different host — and **exactly wrong for Longbox**, which runs on `intentsolutions`. | `grep -c 'recipient:' …/buzz.borg-escrow.sops.yaml` → `2`; recipients `age1me3v…` (estate) and `age1csyj…` (VPS host) |
| **E9** | **The VPS archive already carries the age key and the database dump together.** E13-D01's finding, filed by the same cannon that filed this bead. | bead `longbox-e5b.13.11`; `intent-os ops/backup/IMPLEMENTATION-MAP.md:23-27` — ⚠ **that include set is itself documented as living on the UNMERGED `fix/consistent-db-dumps-in-backup` branch** (`:23`), so this row carries the E12–E15 caveat too: **committed configuration, not observed on the host, and not even on a merged branch** — **AMENDED 2026-09-05 (v1.0.4): the UNMERGED-BRANCH half of that caveat is RETIRED.** The include set is now read from **`intent-os ops/backup/borg-backup.sh` on `main`**, the runner itself rather than a description of one, and `IMPLEMENTATION-MAP.md` §1 was re-pointed at it in PR #576 (squash SHA E13D01_SQUASH_SHA). **The E12–E15 class caveat still stands and is restated, not dropped: committed configuration, NOT observed on the host.** The finding this row states — that the age key and the dumps ride together — remains TRUE of every live archive, because the separation is in the repository and on no box |
| **E10** | **The estate key's own custody has no immutable offsite copy.** The B2 leg covers the VPS repo; *"the dev box's own data — beads/Dolt, crypto custody, host config — has no immutable copy anywhere"*. | `intent-os ops/backup/README.md:24-25`, `:93` |
| **E11** | **019 T22 does not reach this class of loss.** Its v1.3.0 scope note states the figures *"describe DATABASE recovery"*. A restore that returns every row and no key passes. | `019:109` |
| **E12** *(config, not observed)* | **F1 — THE TRANSITIVE CHAIN. Excluding the VPS host key removes the direct path and not the reachability.** Five hops, each a committed line: (i) VPS root reads `/etc/intentsolutions/age.key`; (ii) that key is a recipient of `ops/host/secrets/secrets.prod.sops.yaml`; (iii) which holds `devbox_borg_passphrase`; (iv) which opens `/backup/devbox-borg-replica` **on the VPS** — rsynced there nightly at 02:45, and the passphrase **alone** suffices because the repo is `repokey-blake2`, so the key material lives IN the repository; (v) whose archive sources include `/home/jeremy` and exclude `.cache` but **not** `.config` — so `~/.config/sops/age/keys.txt`, the estate private key, is inside it. | (ii) `intent-os/.sops.yaml:52-56`; (iii) `secrets.prod.sops.yaml:31` (`devbox_borg_passphrase: ENC[…]`); (iv) `~/bin/devbox-borg-push.sh:26` → `DEST="${DEVBOX_BORG_PUSH_DEST:-/backup/devbox-borg-replica}"`, `:14`; (iv) `intent-os ops/backup/IMPLEMENTATION-MAP.md:111` → *"**Encryption:** borg `repokey-blake2`"*; (v) `~/006-scripts/backup-system.sh:31` excludes `/home/jeremy/.cache`, `:51` sources `/home/jeremy`, and `grep -c "exclude '/home/jeremy/.config'"` → `0` — **corroborated independently** by `IMPLEMENTATION-MAP.md:114`, which names the dev age key at `~/.config/sops/age/keys.txt` (`age1me3v…`) in the same paragraph |
| **E13** *(config, not observed)* | **F2 — A GENERIC `path_regex` WOULD ENCRYPT THE ESCROW TO THREE RECIPIENTS, ONE OF THEM UNATTRIBUTED.** `intent-os/.sops.yaml` ends with `(^\|/)secrets\.[a-z]+(\.sops)?\.yaml$` → estate key, **VPS host key**, and `age197narf…`. An escrow named by the ordinary convention matches it. | `intent-os/.sops.yaml:52-56` |
| **E14** *(config, not observed)* | **`age197narf…` is an unattributed recipient and the estate books it as a P0.** *"That key can decrypt every production secret in the estate. Custody cannot be governed while one holder of the master recipient list is unidentified."* Narrowed 2026-08-09 to *not* any estate host key; the next discriminator is the CI `SOPS_AGE_KEY` public half. | `intent-os ops/host/secrets/KEY-CUSTODY.md:88-98`, `:115`, `:156` |
| **E15** *(config, not observed)* | **F3 — THE SECOND RECIPIENT IS AN EXISTING ESTATE OBLIGATION, NOT A NEW LONGBOX QUESTION**, and the estate key's own escrow is UNCONFIRMED. Row 13: *"restore-only age key — **does not exist** — `offsite-backup.config.json` still reads `restore_only_age_key_ref: TO-CREATE`"*, `spine-u1a.2.3` half-open. Row 10, the estate key: *"Bitwarden + printed — **status unconfirmed**"*, and *"cannot be age-escrowed (bootstrap exception)"*. | `KEY-CUSTODY.md:48` (row 10), `:51` (row 13); `offsite-backup.config.json:106` |

**What E4, E5 and E6 mean together, and it is the finding that decides the record.** The documented fallbacks compose badly. A ring loss is survivable *because recovery codes exist*; recovery codes are peppered, so a pepper loss takes them; a pepper loss is survivable *because an owner resets PINs in a privileged session*; a privileged session needs a second factor that is under the ring; and the Longbox-operated break-glass that backstops all of it is held by a role whose own second factor is under the same ring. **Each fallback is real in isolation and every one of them is inside the blast radius of a single lost file** — because all three secrets are destined for one SOPS file rendered onto one host through one `ExecStartPre`. The system's recovery story is written as though these were three independent failures, and the custody design makes them one.

**And what E12–E15 add, which is the harder half.** v1.0.0 answered *where should the escrow live* with a recipient exclusion and called the result separation. **The estate's own committed configuration says the exclusion is not separation** — it removes one hop and leaves four — **and that the recipient list an escrow would inherit by default includes a holder nobody can name.** Neither finding reverses the ruling. Both change what the ruling may CLAIM, and both move the decisive control out of prose and into §8's contract table, which is precisely what the security lens's dissent (§14.1) says was missing.

---

## 3. The cannon — two lenses, DISPATCHED

**The parent session dispatched this record to `security-auditor` and `joe-armstrong-reviewer`.** Both returned **ACCEPT-WITH-CHANGES** — the security lens with thirteen findings (F1–F13, **two CRITICAL, four HIGH**), the availability lens with four (A1–A4). **Seventeen returned, SIXTEEN requiring change, all applied, none declined — F13 was returned clean and is counted as returned rather than as applied.** Both lenses' positions are preserved below whether or not they prevailed, and **the two dissents that survived the rulings are preserved verbatim in §14** — which is the section to re-read first if this record is ever reopened.

### 3.1 Lens S — the application-security position

*"Start with what an escrow IS, because the word does most of the damage. An escrow is a second copy of a secret, created deliberately, kept somewhere the first copy's loss does not reach. Every argument for it is an availability argument and every argument against it is a confidentiality argument, and pretending otherwise is how this decision gets made on vibes.*

*So the confidentiality cost, stated first and not buried: **a second copy is a second thing to steal.** Today the pepper exists in one SOPS file and one tmpfs render. Escrowed, it exists in a third place, with a third recipient set, in a repository with its own history and its own backups, and every future revision of that file still holds it — 050 §5(b)'s residual, arriving a second time for a value 050 never named. That is not a reason to refuse; it is the price, and it has to be in the record beside the benefit.*

*Now the benefit, and it is not the one the bead names. The bead frames ring loss as an **availability** risk — every shop re-installs, every owner re-enrols. For the connector ring that framing is exactly right and the cost is bounded. **For the pepper it is wrong.** A pepper loss does not degrade an operator's access, it ends it, simultaneously for every operator, every recovery code, and — once E03-D11 lands `user_credential` — every password. There is no in-band path back in, because every path back in is authenticated by something under the value that was lost.*

*And the reason I will not accept `.env.example`'s comfort sentence — "the way back in is 048 §8's recovery codes" — is E4. **The codes are peppered.** That sentence is true for a ring-only loss and false for the loss anyone actually has, which is *the SOPS file went with the host*. A recovery story that assumes independent failures of three values living in one file is not a recovery story. **And the coupling is not even a decision** — `hashRecoveryCode` delegates to the PIN helper, so the fallback inherited a dependency nobody argued for. That is F8, and it is fixable: make the codes long and hash them without the pepper.*

*Now the two findings that change what this record may claim rather than what it decides.*

***F1, and I want it in the record as a chain rather than as a caveat.** §5.2's ground for the estate recipient was that it "is off the deploy host". That is a statement about where the key sleeps. The question is what can wake it, and the estate's own committed configuration answers: VPS root reads the host age key; the host age key is a recipient of the production secrets file; that file holds the dev box's borg passphrase; that passphrase opens the dev box replica **which is rsynced onto the VPS every night**; and that archive contains `/home/jeremy/.config`, which is where the estate private key lives, because the exclude list covers `.cache` and not `.config`. **Five hops, no exploit, nothing observed on a host — every one of them a committed line.** RULING 3 removes the first hop and calls the result separation. I am not asking for the ruling to be reversed; I am asking for the sentence to stop over-claiming and for the transitive half to be owned by a bead.*

***F2, and this one is a precondition rather than a caveat.** If this escrow is filed under intent-os's ordinary `secrets.*.sops.yaml` name it matches a generic `path_regex` that encrypts to the estate key, the VPS host key **and `age197narf…`** — a recipient the estate's own custody register describes as appearing in no document, decision record or commit message, able to decrypt every production secret, recorded as an open P0. **So the default naming would create the escrow readable by the exact host we excluded, plus a holder nobody can identify.** The buzz block shows the fix because it already does it: its own explicit `path_regex`, an exhaustive recipient list, and a comment saying which key is excluded and why. And I want the recipient set tested rather than written down, because a recipient list is the whole control and prose is not a control.*

*My position: **escrow, all three, its own path_regex block, an exhaustive and TESTED recipient list, and a drill that opens a purpose-minted row rather than a real person's second factor.** And write down that we are buying availability with confidentiality, at a price, deliberately."*

### 3.2 Lens A — the availability-and-failure position

*"I want to argue about what happens at 09:00 on a Tuesday, because that is when this gets tested and nobody will be reading a decision record.*

*First, the shape of the failure matters more than its probability, and we keep discussing probability. A lost key is not a degraded system. It is a system that comes up **healthy** — the process starts, the port answers, the database restores inside T22 — and refuses every credential it holds. There is no partial mode. **The monitoring cannot see it**, because nothing in the heartbeat set opens a sealed row, and E11 says the one threshold that sounds like it covers this explicitly does not. A failure the instruments report as health is the class I care about most, and this one is in it. That is why I want F6's canary: a fixed known-plaintext probe under every live key version at boot and at readiness. I7 catches two variables holding the **same** value; nothing at all catches a variable holding the **wrong** value, which is what a bad render or a half-finished rotation actually produces.*

*Second — and this is where I part company with the bead's framing — **"every shop re-installs" is not a Longbox act, and that is what makes it expensive rather than cheap.** Longbox cannot perform it, schedule it, batch it or verify it happened. It is N merchant conversations, each of which is Longbox telling a shop that Longbox lost something, and it arrives all at once because the cause is one file.*

*Third, on Option B, because I do not want it dismissed: **accepting a risk is a legitimate engineering answer.** The condition is that the acceptance is (a) explicit, (b) sized, (c) held by somebody who can bear it, and (d) told to the people it lands on. For the connector ring, B passes all four. **For the pepper, B fails (b) and (d)** — nobody has costed a hand rebuild of a shop's identity substrate, and the honest sentence is one no charter should carry as a permanent posture. **Which is exactly why an interim needs a boundary**: an accepted risk with no expiry and no cohort limit is not an interim, it is the plan (A2, and the security lens says it harder in §14.1).*

*Fourth, the coupling I want broken regardless of the escrow decision (A1). `loadConfig` refuses to boot on **any** missing loss-bearing secret. For the pepper and the authenticator ring that is right — they gate identity for everyone. For the connector ring it is wrong: its loss is bounded and shop-recoverable, and coupling it to boot means a connector-ring rotation slip or a partial `ExecStartPre` render takes down **PIN sign-in at the counter**. That is the cheap subsystem's outage propagating into the expensive one, and it is the failure mode I would bet actually happens first. Filed as **E13-D02** `longbox-e5b.13.12`.*

*Fifth, the thing I insist on regardless of which option wins: **an escrow that has never been opened is a belief, not a backup.** The buzz drill is the right shape precisely because it destroys the key in a copy first, and its own header records that a careless drill re-pinned host state and would have silently broken the nightly timer. Copy the discipline, not just the existence. And the acceptance test is not "the file decrypts" — it is **"a real sealed row opened and a real PIN verified using only escrow-derived material"**. **But a cadence is a norm, and norms decay**: I want the freshness gate mechanical (A3), a release refused when the recipient set has changed since the last drill-passed record. And until E00-B03 rules on T22, **the drill's PASS bar is binary — it opened, or it did not — never time-bound** (A4), because a time-bound bar invites somebody to report it against a threshold that does not cover this.*

*Sixth: E10 and E15 together say the estate key's own escrow is unconfirmed and the restore-only key does not exist. So a single-recipient escrow moves the single point of failure rather than removing it. I will take that trade — it is strictly better than today — but it is not a resolved custody story and the record must not call it one.*

*My position: **escrow the pepper and the authenticator ring on the strength of the failure SHAPE, not its probability; the connector ring is a genuine Option-B candidate and I would still escrow it, because it rides in the same file.** The drill is not optional, the canary is not optional, the boot coupling should be split, and the interim needs a boundary and a last-resort procedure — because right now there is no written answer to "it happened; what do we do this morning?"*"

### 3.3 The seventeen findings and where each landed

| # | Class | Disposition |
| --- | --- | --- |
| **F1** | CRITICAL | **UPHELD.** RULING 3 narrowed; chain as §5.2's first row, marked config-not-observed; transitive reach owned by **E13-D01** jointly with the estate spine fabric |
| **F2** | CRITICAL | **UPHELD.** §5.1 requires an own `path_regex` block; §5.2 answers `age197narf…` **NO** and makes it a **PRECONDITION**; §8 **I9** tests the recipient set |
| **F3** | HIGH | **UPHELD.** §9 item 1 re-homed to `KEY-CUSTODY.md` row 13 / `spine-u1a.2.3`; §5.3 states Option A's benefit is unproven until row 10 is confirmed by measurement |
| **F4** | HIGH | **UPHELD.** §5.4's drill opens a purpose-minted drill row, never a real operator's; tmpfs only |
| **F5** | HIGH | **UPHELD.** RULING 4 gains the cohort boundary; RULING 6 gains the enrolment-screen disclosure; §8 **I11** |
| **F6** | HIGH | **UPHELD.** §8 **I10** — the key-liveness canary |
| **F7** | MED-HIGH | **UPHELD.** §8 **I2** rewritten to test the escrow file's plaintext key names, not a local inventory |
| **F8** | MED-HIGH | **UPHELD, position taken.** §9 item 4a recommends branch (a) to **E03-D11**, with branch (b)'s consequence stated and the ergonomics tension named |
| **F9** | MED | **UPHELD.** §9 item 4b, corrected sentence supplied to **E03-D11** |
| **F10** | MED | **UPHELD.** §8 **I7** gains its limits and an entropy floor |
| **F11** | MED | **UPHELD.** `KEY-CUSTODY.md` is THE register; the Longbox-local inventory is dropped; §9 item 7 files the intent-os obligation |
| **F12** | LOW | **UPHELD.** §9 item 8 — whether the three should share one operational file at all |
| **F13** | — | Clean |
| **A1** | — | **UPHELD.** Cited to **E13-D02** `longbox-e5b.13.12`; §9 item 9 |
| **A2** | most costly | **UPHELD.** New **§6.1 Last resort**; full runbook to **E13-D01** |
| **A3** | — | **UPHELD.** §8 **I4/I5** gain the mechanical freshness gate |
| **A4** | — | **UPHELD.** §8 **I6** — binary PASS bar until E00-B03 rules |

---

## 4. Ruling — the acting head's, under Jeremy's standing delegation

> **RULING 1 — OPTION A, ESCROW, AND IT COVERS ALL THREE SECRETS AND NOT THE TWO RINGS THE BEAD NAMES.** The bead asks about `LONGBOX_CONNECTOR_KEY_V<n>` and `LONGBOX_AUTHENTICATOR_KEY_V<n>` and invites deciding both rings in one record. **The record decides three**, adding `LONGBOX_PIN_PEPPER`, on E4/E5/E6's ground.
>
> **RULING 2 — THE UNIT OF ESCROW IS THE CUSTODY PATH, NOT THE SECRET.** The three values reach the process through one SOPS file, one `ExecStartPre`, one tmpfs render, on one host. Their loss is **one event with three consequences**. **AMENDED at v1.0.1 (F12): that correlation is MANUFACTURED by a custody choice and is not given.** It is the right unit for the escrow decision *because the custody choice has already been made*, and whether it should have been — three secrets in one operational file — is a separate question this record does not settle. §9 item 8.
>
> **RULING 3 — THE BUZZ PRECEDENT IS ADAPTED, NOT COPIED, AND THE ADAPTATION IS THE RECIPIENT SET. NARROWED AT v1.0.1 (F1).** The mechanism transfers whole. The recipient list inverts: buzz excludes the buzz host key and includes the `intentsolutions` host key; **Longbox must exclude the `intentsolutions` host key**, because Longbox runs there (E8) and because that host's age key and this system's ciphertext already share one archive bound for an Object-Locked bucket (E9).
>
> > ⚠ **THE NARROWING, AND IT IS THE REASON §10 WAS NOT SIGNABLE AS v1.0.0 STOOD.** v1.0.0 read this exclusion as separation. **It is not.** E12 traces five committed hops from VPS root to the estate private key, and excluding the host key removes only the first. **What this ruling establishes is that the DIRECT reach is removed. The TRANSITIVE reach is NOT removed by anything in this record, and it is owned by E13-D01 jointly with the estate spine fabric** (`spine-u1a.*`), because three of the five hops are estate configuration Longbox does not own. **No artifact may describe the escrow as separated from the deploy host** until the transitive chain is closed or a compensating control is proven — the honest sentence is *direct reach removed, transitive reach open and owned*.
>
> **RULING 4 — THE ESCROW DOES NOT EXIST YET, AND OPTION B IS THE HONEST INTERIM POSTURE UNTIL IT DOES. BOUNDED AT v1.0.1 (F5).** E3: there is no SOPS file in this repository to escrow FROM, and E9's separation is E13-D01's unfinished work. Until both land, **the deployed posture is Option B and this record says so in the present tense**.
>
> > **THE BOUNDARY, because an accepted risk with no expiry is not an interim (A2, §14.1).** **No shop beyond Pilot A enrols an operator or completes a connector install until the escrow file exists with its ratified recipient set and RULING 5's drill has passed once.** Pilot A's interim is permitted **because RULING 6's sentence is delivered to a named signer** — one shop, one charter, one person who was told. That condition does not scale by repetition, and §8 I11 makes the boundary a check rather than an intention. **The beads it binds are named so it is enforceable rather than rhetorical: `longbox-adk.7` (E16-B06, *remediate P0/P1 findings and execute the 100-item mixed batch*) and `longbox-adk.8` (E16-B07, *execute cumulative ≥300 items across ≥2 operators and ≥4 weeks*)** — the two beads that expand beyond one shop's counter, and therefore the two this boundary stops until the escrow is real.
>
> **RULING 5 — THE ACCEPTANCE TEST IS AN OPEN, NEVER A DECRYPT.** An escrow is proven when escrow-derived material alone opens a sealed `user_authenticator` row and verifies an `operator_pin`, in an isolated environment, against a copy. **AMENDED at v1.0.1 (F4): the row it opens is PURPOSE-MINTED and is never a real operator's.**
>
> **RULING 6 — THE PILOT IS TOLD, AND TOLD THE TRUE SENTENCE.** Under 022 P3 and P8 the shop is told what a Longbox failure costs them in their own terms. The charter fill-in item handed to **E01-B05** is not *"we escrow our keys"* — during the interim that is false — it is: **if Longbox loses its key material, the shop's staff sign-ins stop working and restoring them requires the shop to re-set every PIN and the owner to re-enrol their second factor, and reconnecting the store requires the owner to re-install the app.** No artifact may state it more softly, and **no registered claim, C-row or partner-facing sentence may describe key custody, escrow or recoverability as a property of this system at any class** until §5 exists and RULING 5's drill has passed (021 B16; T26 pre-send).
>
> > **AMENDED at v1.0.1 (F5): the disclosure attaches at a SECOND point, and it is the one that reaches the person who bears it.** A charter is signed once by an owner who may not be the person enrolling a second factor eleven months later. **The same sentence, in operator-facing words, attaches at the owner's TOTP enrolment screen** — the moment a person is asked to trust a factor with a recoverability property. Filed as an **E05-D02 `longbox-e5b.5.12`** surface with a 021 candidate C-row under the T26 pre-send; the copy is not written here, because operator copy is selected from a code and never authored in a decision record (042 §2.5's discipline).
>
> **AND THE CHARTER HALF IS FILED WHERE A CHARTER FILL-IN GOES, following 037's precedent.** The disclosure is a **038 §6 fill-in row** — 038 §6 is the *"Counsel checklist — the batched engagement, with exact draft-language locations"*, which is where a sentence that must reach a signer is queued with its location rather than being asserted into being by a decision record. Its shop-facing home is **039**, the shop-facing charter template. **This record supplies the OBLIGATION and its content, not the rendered clause**: 039's language is written in the shop's words, and 021 B16 governs what may be said at what class.
>
> **RULING 7 — THE THREE VALUES MUST DIFFER, AND NOTHING CHECKS THAT TODAY.** 053's `.env.example` argues at length that the connector ring is *"deliberately a different variable"*, because one key for two subsystems makes one compromise two. **That argument is about VALUES and the mechanism is a VARIABLE**, so an operator who pastes the same 32 bytes into both satisfies every check in the tree. §8 I7 closes it — **and I7 is a typo-catcher, not an independence proof** (F10): §8 states its limits rather than letting it read as more than it is.

**What is NOT ruled, deliberately.** Nothing here says a cadence is a control, nothing here says an escrow makes a loss unlikely, and nothing here converts an availability risk into a durability claim. The escrow changes **who can restore**, not **how often the file survives** — and after F1, it changes the direct reach and not the transitive one.

---

## 5. Decision A, as built — the escrow's shape

**5.1 The artifact, and its own `path_regex` block (F2).** One SOPS-encrypted file in the estate ops tree, committed to git, holding the escrowed values keyed by their environment-variable NAMES so an operator restoring under pressure does not have to guess which blob is which. **It is not in this repository**: this repository's SOPS file (once E3 is closed) is the OPERATIONAL source, and an escrow committed next to the thing it insures shares its custody path, its clone, its backup and its loss event.

> **PRECONDITION, not a preference.** The escrow **must be given its own explicit `path_regex` block in `intent-os/.sops.yaml`, in the buzz escrow block's form** — an exhaustive recipient list and a comment naming the excluded key and the reason. The form to copy is `intent-os/.sops.yaml:32-41` — six comment lines naming the excluded key and the circularity, then the `path_regex` and its two recipients. **It must NOT be filed under a name matching the generic `(^|/)secrets\.[a-z]+(\.sops)?\.yaml$` block** (E13), which would encrypt it to the estate key, the VPS host key **and `age197narf…`** — creating an escrow readable by the very host RULING 3 excludes, plus an unattributed holder (E14). **A creation step that gets the filename right and the block wrong produces a file that looks correct and is inverted**, which is why this is a precondition and why §8 I9 tests it.

**5.2 The recipients, and the exclusions that are the whole point.**

| Recipient | In the escrow? | Why |
| --- | --- | --- |
| The estate age recipient (`age1me3v…`), held on the dev box at `~/.config/sops/age/keys.txt` | **YES** — but **NOT** on v1.0.0's ground | v1.0.0 said *"it is off the deploy host"*. **F1 withdraws that sentence.** E12's five hops reach it from VPS root anyway. The honest ground is narrower: **it is the only recipient today whose holder is a person rather than a host, and excluding the deploy host key removes the DIRECT path** — which is a real reduction and is not separation |
| The `intentsolutions` VPS host key (`age1csyj…`) | **NO — EXCLUDED** | Longbox runs on that host (E8), and that host's key and this system's ciphertext already ride one archive to an Object-Locked bucket (E9) |
| `age197narf…` — the recipient the estate's custody register carries as unattributed and P0 (E14) | **NO — EXCLUDED, and its exclusion is a PRECONDITION** | *"That key can decrypt every production secret in the estate… custody cannot be governed while one holder of the master recipient list is unidentified."* **No Longbox escrow is created while an unattributed recipient can match its path.** Identification is the estate's open question (`KEY-CUSTODY.md:156`); this record does not remove the key and does not assume it benign — it refuses to file under a path the key matches |
| **The transitive chain itself** *(config, not observed — E12)* | **NOT CLOSED BY THIS RECORD** | VPS root → host age key → `secrets.prod.sops.yaml` → `devbox_borg_passphrase` → `/backup/devbox-borg-replica` on the VPS (rsync 02:45) → `~/.config` in the archive → the estate key. **Owned by E13-D01 jointly with the estate spine fabric.** The cheapest candidate fix — excluding `~/.config/sops` from `backup-system.sh` — is an estate change with its own consequences and is **not** prescribed here. **AMENDED 2026-09-05 (v1.0.4): IT IS NOW PRESCRIBED, AND THE CHAIN IS STILL OPEN.** PR #576 (squash SHA E13D01_SQUASH_SHA) excludes `/home/jeremy/.config/sops` from the dev-box runner and refuses the run when that exclusion is missing — so the chain's LAST hop (*`~/.config` in the archive → the estate key*) is closed **in the repository and on no host**, which is the only place a test can close it. The four hops upstream were never this bead's to close. **Two consequences this record must carry rather than imply:** the exclusion makes custody row 10's *unconfirmed* escrow the estate key's only forward copy, so §9 item 2's measurement becomes a PRECONDITION of deploying the dev-box half rather than merely owed; and pre-deploy archives keep the key for each leg's retention — roughly six months under `--keep-daily 7 --keep-weekly 4 --keep-monthly 6`, not the 30 days of B2's Object Lock. **Nothing here may be read as *separated*** — RULING 3's narrowing is untouched |
| A second, non-VPS recipient | **OWED — and it is an EXISTING estate obligation, not a new question (F3)** | `KEY-CUSTODY.md` **row 13**: a restore-only age key *"does not exist"*, `offsite-backup.config.json:106` still reads `restore_only_age_key_ref: TO-CREATE`, tracked as **`spine-u1a.2.3`** (half-open, design in `spine-u1a.12.4`). §9 item 1 routes there rather than opening a parallel Longbox question |

**5.3 Who holds the recipients — and why Option A's benefit is UNPROVEN until that is measured (F3).** The estate recipient's private half is held by Jeremy on the dev box. **That is a one-person custody story and this record calls it that.** Worse, it is a one-person custody story whose own backup is unconfirmed: `KEY-CUSTODY.md` row 10 records the estate key's escrow as *"Bitwarden + printed — **status unconfirmed**"* and notes it *"cannot be age-escrowed (bootstrap exception)"*.

> **Therefore: until row 10 is confirmed BY MEASUREMENT — somebody opens the Bitwarden entry or the printed copy and proves it is the current key — Option A's availability benefit is UNPROVEN**, because the escrow's only opener has no verified backup of its own. An escrow whose sole key can be lost the same way the original was is a copy, not an insurance policy. **And the drill may not be run only on the box that holds the sole opener**: a drill executed exclusively there proves the estate key works where it already lives, which is the one condition the disaster removes.

**5.4 The drill — a PURPOSE-MINTED row, never a real operator's (F4).**

v1.0.0's drill decrypted a real person's live TOTP secret against a full copy of the identity table, on a recurring schedule, with no 022 P7 record. **That is a routine, scheduled decryption of staff credentials dressed as a safety control, and it is refused.** The drill follows the buzz script's discipline — redirect every piece of tool state into a throwaway directory, because a drill that re-pins host state silently breaks the live path it was meant to protect — with this shape:

1. **Mint the subject.** A drill-only `app_user`, enrolled through the **live code path** under the **live key version**, with a drill-only PIN. **One drill row per live key version**, so a rotation that adds `_V2` gets its own subject and the drill covers what is actually in use.
2. Take a COPY of the database (never the live one) and a throwaway environment.
3. **Render the escrow to tmpfs only** — 050 §3's rule inherited without exception; the escrow is never decrypted to persistent disk.
4. **Open the drill row**: decrypt the drill `user_authenticator` ciphertext under the escrowed ring and verify the drill `operator_pin` under the escrowed pepper, using no material reachable from the deploy host.
5. Assert the drill can see no live environment file and writes nothing outside its temp directory.
6. Record a PASS/FAIL and the commit, with **no value and no digest of a value** in the log.

**A real operator's row is never opened, at any cadence, for any reason** — and the reason is not only 022 P7. It is that the drill exists to prove a key opens a lock, and enrolling a lock for the purpose costs nothing.

> **The cadence figures are PROVISIONAL FLOORS in 042 §8.4's class and are declared NON-EVIDENTIARY**: scheduling defaults chosen for review, not measurements, supporting no claim about recoverability at any class (021 B16). **Once before G3**, and thereafter on **046 §8.1's trigger rule** — when a loss-bearing secret is added or removed, when the recipient set changes, when the deploy host changes, or when the rendering mechanism changes — with a **PROVISIONAL 90-day** floor so a trigger-only rule cannot silently mean never. **A cadence is a norm and norms decay, so §8 I4/I5 make the freshness gate MECHANICAL (A3)** rather than trusting the schedule. Neither figure may be quoted as a recovery-time property, and neither belongs in T22 (E11).

---

## 6. Decision B, as it would have been recorded — and why it is the interim rather than the answer

B is not dismissed, because B is what is true today (RULING 4) and because Lens A's four conditions are the right test. Applied per secret:

| Secret | Failure if lost | Remedy | Can B's four conditions be met? |
| --- | --- | --- | --- |
| `LONGBOX_CONNECTOR_KEY_V<n>` | Every shop's connector token is unopenable; drafting refuses (053 §4) | Every shop re-installs the app — a merchant act Longbox cannot perform | **YES.** Explicit, sized, bearable, statable in one sentence. **A genuine Option-B candidate** — escrowed anyway under RULING 2, because it rides in the same file. **And its BOOT coupling is wrong regardless (A1)**: §9 item 9 |
| `LONGBOX_AUTHENTICATOR_KEY_V<n>` | Every second factor is unopenable for `owner`, `manager` and `support_break_glass` | Recovery codes — **if and only if the pepper survives** (E4) | **CONDITIONALLY.** True for a ring-only loss, false for the loss that actually happens, which is the file. **F8 would make it unconditionally true** by removing the coupling |
| `LONGBOX_PIN_PEPPER` | Every operator PIN, every recovery code and (once E03-D11 lands) every password is unverifiable, simultaneously | None from inside the system. Every in-band path is authenticated by something under the lost value (E4, E6) | **NO.** Fails "sized" and fails "told" |

**The residual under B, stated because B is the live posture:** today, a loss of the deployment's secret material means the shop's staff cannot sign in, the owner cannot pass a second factor, no recovery code redeems, the break-glass operator's own factor is unopenable (E6), and the connector is dead — while the database restores perfectly, inside T22, with the heartbeat green (E11). **That is the accepted risk. It is held by the acting head, it is bounded by RULING 4's cohort limit, E01-B05 carries RULING 6's words to the shop, and §6.1 says what is done when it happens.**

**The residual under A, stated because escrow is not free:** a second copy of three secrets exists in a second repository with its own history and its own backups; every future revision of that file still holds the values (050 §5(b), reaching a value 050 never named); the recipient set is a one-person custody story whose own backup is **unconfirmed** (§5.3); the **transitive chain is open** (E12); and **the escrow defends a lost file, not a compromised process** — 053 §14's sentence transfers unchanged.

### 6.1 Last resort — what is done on the morning it happens (A2)

A2 is recorded as the availability lens's most costly finding: **the break-glass is inside its own blast radius (E6) and no last-resort procedure exists anywhere.** This is a stub, deliberately labelled as one, so the gap is owned rather than discovered during the incident. **The full runbook is E13-D01's**, noted on that bead. **AMENDED 2026-09-05 (v1.0.4): THE RUNBOOK NOW EXISTS — `intent-os ops/longbox/RUNBOOK-ring-loss.md`** (E13-D01, PR #576, squash SHA E13D01_SQUASH_SHA). It carries what this stub routes away: who acts and why it cannot be a support role, the escrow branch with its two preconditions that are both still false, the rebuild order when the escrow does not open, and what is said to a shop — in 022 P3/P8's language, with **no statute cited and no recovery implied**. **The stub's five answers are unchanged and remain the summary of record**; the runbook elaborates them and contradicts none.

| Question | Answer today |
| --- | --- |
| **Who acts?** | The **acting head**. Not a support role — every `support_break_glass` second factor is under the lost ring (E6), so the role that exists for this cannot be assumed |
| **With what, if the escrow opens?** | Escrow-derived material plus direct database access: re-render the environment, restart, and the system resumes with every credential intact. **This branch requires the escrow to exist and its opener to be available — neither is true today** |
| **With what, if it does not?** | **Nothing that restores the credentials.** Direct database access can retire every live authenticator and session and re-enrol from scratch — an owner re-enrols TOTP, every operator gets a new PIN in person (048 §8.3), every shop re-installs the connector. **There is no path that recovers the sealed values, and this record will not imply one** |
| **Who is told, and when?** | **Every affected shop, before they discover it at the counter**, in RULING 6's words. The sequence beyond the charter sentence — who calls, in what order, what the shop does that morning, what Longbox does not ask them to do — is **not written here and is E13-D01's**, with 022 P3/P8 governing the tone and 021 B16 governing what may be said |
| **What must never happen?** | A quieter second path. 048 §8.2's rule holds under a total loss: no super-admin, no reset affordance for Longbox staff, and every recovery act an append-only fact the owner sees |

---

## 7. What this record does NOT change

It changes no ratified record, amends no invariant, moves no threshold, files no migration, adds no route, and closes no 046 row. **048's §12.4 row 3 is not rewritten** — the orphaned hand-off is recorded in E5 and re-homed by §9 rather than edited into a different bead's name. **053 is not amended — and the bead's own acceptance clause is DISCHARGED HERE rather than there, deliberately.** E03-D12's text offers *"record 'ring loss = every shop re-installs' as an accepted availability risk in **053 §14**"*. **That half is discharged by 055 §6** — the per-secret table, the residual paragraph and §6.1 — and NOT by editing 053, for two reasons. First, 053 is **PROPOSED on an unmerged branch**: amending another bead's unratified record from this branch would either conflict on merge or silently ratify a sentence its own gate audit has not seen. Second, the acceptance is no longer 053's shape — it covers **three secrets**, and a row in the connector record would split one decision across two documents, which is the drift E5 is a record of. **053 §14's sentence stays correct and unedited**; this record supplies the escrow it presumes. **019 T22 is not extended here** — §8 I6 states the contract and E13-B07 owns the instrument (019's change control is E00-B03's). **`intent-os` is not edited by this commit**: §5.1's `path_regex` block and §9 item 7's `KEY-CUSTODY.md` rows are obligations filed on named beads, not changes made here — a record that reached into another repository's custody register would be the drift this project keeps catching.

---

## 8. Contracts the code half must satisfy

Every row is an obligation on a named bead. **None is tested today**, and this record ships no test. **I9 and I10 are the two that answer the security dissent's central charge** (§14.1): that the whole decision reduces to a list of recipients and v1.0.0 contained no test of a list of recipients.

| # | Invariant | Test it will get | Owner |
| --- | --- | --- | --- |
| **I1** | **The loss-bearing secrets are ONE exported list, not three scattered `require*` calls**, and every member is checked at boot **in its declared failure class** (A1: hard-refusal vs degrade). Adding a fourth secret without adding it to the list fails the build | `tests/config-fail-closed.test.ts` — a case asserting the list is exactly the set of variables `loadConfig` treats as loss-bearing, walked from the list rather than named | **E13-B02**, with **E13-D02** |
| **I2** | **The ESCROW is tested, not an inventory (F7).** SOPS leaves top-level keys in plaintext, so the escrow file's key names are readable without decrypting: the symmetric difference between them and the boot-required secret list must be **empty**. Adding `..._V2` to the environment without adding it to the escrow **fails the build** | A contract test reading the escrow's plaintext key names (no decryption, no age key in CI) and comparing to I1's list | **E13-D01**, with **E13-B02** |
| **I3** | **No escrow artifact, register row, drill log or receipt contains a secret value, a prefix, a last-four or any digest offered as a hint** (053 §3.3, extended to escrow artifacts) | `pnpm exec audit-harness escape-scan` over the escrow and drill paths, plus a `tests/contract/secret-surfaces.test.ts` case asserting every register value is a `sops://` reference or a fingerprint and nothing else | **E13-D01** |
| **I4** | **The restore drill proves an OPEN, not a decrypt**, against a **purpose-minted drill row per live key version** (F4), decrypting only to tmpfs, never a real operator's row. **Plus a MECHANICAL freshness gate (A3):** a CI check hashes the current recipient set and refuses a release when that hash differs from the one on the last drill-passed record | The drill's own acceptance run recorded as evidence with its commit; the CI freshness check, in T34's heartbeat-record pattern | **E13-B07** |
| **I5** | **The drill writes nothing outside its temp directory and cannot reach the live rendered environment file** — the buzz host-state lesson generalised — and its freshness record is a fact, not a log line somebody can edit | An assertion inside the drill that the live `EnvironmentFile` path is unreadable in its context and that its temp directory is the only write target | **E13-B07** |
| **I6** | **The drill asserts key availability, not only database restore**, and **its PASS bar is BINARY — it opened, or it did not — never time-bound (A4)** until E00-B03 rules on T22. A drill that returns every row and no key must FAIL and must not be reportable against T22 | The drill's acceptance criteria; a **019 §9.4** amendment row (§9.3 is taken — E04-D04's A8 row at 019 v1.4.0) is **considered and DECLINED: no row text is supplied here.** The three prior records that reached a 019 line supplied their text and refused to write it; **this one does not supply text either**, because the question *what does a threshold about database recovery owe a key-availability failure* is a threshold-design question rather than a transcription, and drafting the row would be answering it. §9 item 14 routes it | **E13-B07** → **E00-B03** |
| **I7** | **The three loss-bearing values are distinct**, and a deployment setting two to the same value refuses to boot. **Its LIMITS are stated (F10): distinctness is not independence, and this catches the paste-twice error only.** Widened to **every secret-classed variable**, and paired with a **minimum-entropy floor for ring variables** beside `MIN_PEPPER_LENGTH` (`src/services/auth/secrets.ts:95`). The refusal message names **variables, never values, prefixes or digests** | `tests/config-fail-closed.test.ts` — the collision case, the distinct-values boot case, the short-ring case, and an assertion that the thrown message contains no substring of any input | **E13-B02**, with **E03-D13** |
| **I8** | **No registered claim, C-row or partner-facing sentence describes key custody, escrow, separation or recoverability as a property of this system** until §5 exists, I9 passes and I4 has passed once. **Explicitly includes RULING 3's narrowing: nothing may say "separated from the deploy host" while E12's chain is open** | The **T26 pre-send check** against 021. Stated as a registry rule, not dressed as a unit test | **E11-B06** owns the rendering of any shop-facing sentence, with **021** the gate at the T26 pre-send — the split 050 §6.5 already draws (*"E11-B06 owns the rendering; this record fixes only that the fact is DERIVABLE… it does not fix that it is reportable, and the difference is 021's"*), adopted here so §8's opening sentence stays true that every row names a bead |
| **I9** *(new, F2)* | **THE RECIPIENT SET IS A TESTED LIST, NOT PROSE.** The escrow's recipient set equals a committed allowlist exactly, and **the deploy host key is not a member**. Testable **without decrypting anything**: SOPS records recipient fingerprints in the file's own metadata, so the check is a fingerprint comparison — no age key in CI, no plaintext anywhere. Shaped on `KEY-CUSTODY.md` rows 10–12 | A contract test comparing the escrow's recorded recipients to the committed allowlist, asserting set equality and asserting the `intentsolutions` host fingerprint is absent. **AMENDED 2026-09-05 (v1.0.4): the BLOCK half has LANDED; the FILE half has not.** `intent-os ops/longbox/secrets/escrow-recipients.allowlist` and `ops/longbox/tests/escrow-recipient-allowlist-test.sh` (PR #576, squash SHA E13D01_SQUASH_SHA, 12 cases in `ci:drills`) assert over the **`.sops.yaml` creation rule** that the declared recipients are a **SUBSET** of the allowlist's `allow` set and **disjoint** from its `deny` set — so `age1csyj…` and `age197narf…` are both provably absent — plus a mutation case proving the deny check can fail, and a case asserting the escrow FILE does not exist. **Subset and not equality is deliberate**: the allowlist outlives the block, because §5.2's still-owed second opener is an `owed` line before it is an `allow` line. **Set equality against the escrow's own SOPS metadata remains OWED** and cannot be run until the file exists, which §5.2's precondition still forbids | **E13-D01** — block half DONE, file half OPEN |
| **I10** *(new, F6)* | **A KEY-LIVENESS CANARY at boot and in readiness.** Decrypt a fixed known-plaintext canary sealed under **each live key version** and verify a known digest under the pepper; **fail readiness on a miss.** Fixed plaintext, no value logged. **Named distinctly from 053 I1** — that one asserts a key is PRESENT; this asserts it is the RIGHT one. I7 catches two variables holding the same value; **nothing today catches a variable holding the wrong value**, which is what a bad render or a half-finished rotation produces | A unit case per key version plus a readiness-probe case asserting a wrong key fails readiness rather than starting healthy | **E13-B02**, with **E13-B07** |
| **I11** *(new, F5)* | **RULING 4's cohort boundary is a CHECK, not an intention.** No shop beyond Pilot A enrols an operator or completes a connector install until the escrow exists with its ratified recipient set and I4 has passed once. **And RULING 6's sentence attaches at the owner's TOTP enrolment screen**, not only in the charter | A registration/enrolment guard test asserting the refusal for a second shop while the escrow-ready fact is absent; the enrolment-screen copy as an **E05-D02 `longbox-e5b.5.12`** surface with a 021 candidate C-row (015 row) | **E01-B05** and **E05-D02 `longbox-e5b.5.12`**, with **E13-D01** |

---

## 9. Not decided here

1. **The escrow's SECOND recipient — re-homed at v1.0.1 (F3).** It is **not a fresh Longbox question**: `KEY-CUSTODY.md` **row 13** records that a restore-only age key *"does not exist"*, `offsite-backup.config.json:106` still reads `restore_only_age_key_ref: TO-CREATE`, and it is tracked as **`spine-u1a.2.3`** (half-open, design in `spine-u1a.12.4`). Longbox's escrow **consumes** that key when it exists and does not design a parallel one. **`spine-u1a.2.3`**, referenced from **E13-D01**.
2. **Confirming `KEY-CUSTODY.md` row 10 BY MEASUREMENT** — that the estate key's Bitwarden and printed copies are current and openable. Until then §5.3 stands: Option A's availability benefit is **unproven**. **An estate obligation**, cited from **E13-D01**.
3. **Closing E12's transitive chain**, or proving a compensating control. Three of five hops are estate configuration Longbox does not own. **E13-D01 jointly with the estate spine fabric.** The cheapest candidate — excluding `~/.config/sops` from `backup-system.sh` — is named as a candidate and **not prescribed**, because a backup exclusion has its own failure mode and the estate owns that trade.
4. **Where the deployment's OPERATIONAL SOPS file lives** — this repository, or the ops tree. 050 §3 says *"the SOPS file in the repository"* without saying which, and E3 says neither exists. **E13-D01**, with the unit file.
   - **4a. THE RECOVERY-CODE / PEPPER COUPLING — a position is taken (F8).** E4 shows the coupling is **inheritance, not decision**: `hashRecoveryCode` delegates to the PIN helper. **RECOMMENDED: branch (a) — recovery codes minted at 128 bits from a CSPRNG and hashed WITHOUT the pepper**, so 048 §8's fallback survives a pepper loss and the composition failure in §2 is broken at its cheapest link. **The tension is stated rather than hidden**: 048 §7.1a's *"the code is never typed, so its length costs nothing"* was written about a **scanned** enrollment code, while 048 §8.1 chose **PROVISIONAL ~50 bits** for recovery codes **precisely because a person types one under stress** (`src/services/auth/codes.ts:129-131` declares the figure *"a **PROVISIONAL floor** (042 A3): stated with its derivation, no measurement behind it, and never quoted as a security property"*, so the comparison below is between a floor and a proposal and neither is a measurement). So branch (a) is not free — it trades typing ergonomics for independence, and it owes 048 §8.1 an amend-by-a-row. **If branch (b) is chosen instead (keep the pepper), the consequence must be written into 048 §8 in terms: recovery codes are NOT a fallback for a custody loss, only for a lost phone**, and §6's middle row stays CONDITIONAL forever. **E03-D11** implements whichever is ruled.
   - **4b. `.env.example:270-273`'s false sentence (F9).** *"The way back in is 048 §8's recovery codes"* is true for a key **removed** in a botched rotation and **false for a file loss**, which takes the codes with it (E4). Corrected sentence supplied for **E03-D11** to apply: *"The way back in is 048 §8's recovery codes — but only if the pepper survived. If the SOPS file itself was lost, the codes went with it (055 §2 E4) and the answer is 055 §6.1."*
5. **Whether the pepper rotates on a cadence, and how.** 048 §9.2 specifies `pepper_version` and re-peppering at next successful verification; nobody has built it. **E03-D11.**
6. **The reseal job that makes ring retirement real.** **E03-D13** `longbox-e5b.3.23`.
7. **Registering Longbox's secrets in THE estate register (F11).** `~/000-projects/intent-os/ops/host/secrets/KEY-CUSTODY.md` is the estate's custody register and **there will be no competing Longbox-local inventory** — I2 tests the escrow itself, not a second list. Adding Longbox's rows to `KEY-CUSTODY.md` is an **intent-os obligation**, filed there and referenced from **E13-D01**.
8. **Whether the three secrets should share one operational file at all (F12).** RULING 2's correlation is manufactured by a custody choice. Splitting the identity secrets from the connector ring into two files with two recipient sets would decorrelate the loss — at the cost of two render paths and two things to get wrong. **E13-D01**, with the unit file, and it is a genuine open question rather than a rhetorical one.
9. **Splitting the boot-refusal gate by failure class (A1).** `loadConfig` refuses to boot on any missing loss-bearing secret; the connector ring's loss is bounded and shop-recoverable and should degrade its own routes instead of taking down PIN sign-in. Filed by the availability lens as **E13-D02 `longbox-e5b.13.12`**, under **LBOX-E13** and **depending on E13-B02** (`longbox-e5b.13.2`) — the dependency edge is recorded on the bead, not merely implied by prose.
10. **The last-resort runbook beyond §6.1's stub (A2).** **E13-D01**, noted on that bead.
11. **Whether `user_credential`'s password hash uses the same pepper value.** 048 §9.2 says it does; the table does not exist. **E03-D11.**
12. **Whether any external obligation constrains key custody or escrow.** **No statute is cited in this record.** Routed to the **E01-B05 stage-2 counsel batch** with technical context attached, alongside 053 §15 item 3.
14. **What 019 owes a key-availability failure, if anything.** T22 bounds DATABASE recovery (E11), and §8 I6 refuses to draft the row that would extend it — a threshold's meaning is 019's to set and **E00-B03** owns its change control (018 §5). Routed there with §8 I6 and E11 as the context, and with the note that the answer may legitimately be *nothing*: a drill that gates a release (I4's freshness check) may be the better instrument than a threshold nobody measures.
13. **Whether escrow should later be replaced by a hosted key service.** E03-B05 is closed and 050 rejected the column, not the question. **The condition to reopen is stated so it can be tested rather than re-argued:** if §9 item 1 and item 2 both stall — no restore-only key, and no measured confirmation of the estate key's own escrow — the one-person custody story is the reason to reopen it.

---

## 10. Ratification

**RATIFIED at v1.0.3 by the acting head (2026-09-04), under Jeremy's standing delegation, after the gate re-audit returned READY at `bcb00d8`.** What is signed is the RULING set and the contract table; §14.1's security dissent stands unanswered on the transitive chain (E13-D01) and is a reopening trigger, not a waived objection. Nothing is built; every §8 contract is owned by a named bead. The prior status text follows for the record: The cannon in §3 was **dispatched**; both lenses returned **ACCEPT-WITH-CHANGES**; **seventeen findings returned, sixteen requiring change, all applied, none declined (F13 clean)**. `longbox-gate-auditor` runs on the record half before the bead closes, and the acting head signs after. **E03-B10's independent security review is required regardless, and closing G2 does not follow from this signature.**

**§10 was NOT signable on v1.0.0's RULING 3 sentence**, and that is recorded here rather than only in the change log: v1.0.0 asserted that excluding the deploy host key separates the escrow from the deploy host, and E12 shows it removes one hop of five. The ruling stands narrowed; the claim does not survive.

**Nothing in this record is built.** No escrow file exists, no `path_regex` block exists, no drill has run, no canary exists, none of the eleven contracts is tested, and the deployed posture is Option B with the residual §6 states and the boundary RULING 4 sets. **The signature would authenticate a decision, a sequence and a set of preconditions — not a control.**

**Standing review.** Redrawn on 046 §8.1's trigger rule: when a loss-bearing secret is added or removed, when the recipient set changes, when the deploy host changes, when the rendering mechanism changes, when a fallback this record relies on is altered by another record, or **when any hop in E12's chain changes**. **Not on a calendar** — and the 90-day figure in §5.4 is a drill floor, not a review cadence.

---

## 14. Dissents preserved verbatim

**The section number mirrors 048 §14.1's dissent-preservation convention** and is not a claim that sections 11–13 were removed; this record has ten numbered sections and this one.

Both lenses returned ACCEPT-WITH-CHANGES and both concurred with the construction. **These are the positions that survived the rulings**, preserved unedited, and they are the first thing to read if this record is ever reopened.

### 14.1 The security lens

> "I concur with the ruling and not with the sentence that carries it. §5.2 says the estate recipient is chosen because it 'is off the deploy host.' That is a claim about where a key sleeps, and the question is what can wake it. … The estate key is three hops from the machine we excluded, and the first hop is the key we excluded it for. RULING 3 removes the direct path and calls it separation. And I will not sign the recipient set as a matter of prose. … The whole decision reduces to a list of recipients and §8 contains no test of a list of recipients. That is not an omission in a contract table. That is the control. … We should not have to be told that the way to test whether a key opens a lock is not to open somebody's front door. … A posture the record's own table rules dishonest, adopted with no expiry, is not an interim — it is the plan."

**Where each clause landed, so the dissent is answered rather than filed.** The *"off the deploy host"* sentence is **withdrawn** — §5.2's first row now states the narrower ground and carries E12's chain beside it. *"A list of recipients and no test of a list of recipients"* is **§8 I9**, which is why I9 exists and why it is fingerprint-based so it can run without an age key. *"Not to open somebody's front door"* is **§5.4's purpose-minted drill row**. *"No expiry — it is the plan"* is **RULING 4's cohort boundary and §8 I11**. **The first clause is not fully answered and cannot be by this record**: the transitive chain is open, three of its five hops are estate configuration, and §9 item 3 owns it. **Until it closes, the dissent stands on that point and I8 forbids any artifact from claiming otherwise.**

### 14.2 The availability lens

> "honest-and-unrecoverable is worse than the record currently signals in its own residual-risk framing."

**Where it landed.** §6's residual tables and §6.1's last-resort stub are the answer, and the answer is that in the no-escrow branch there is **nothing that restores the credentials** — stated in those words rather than softened into a recovery procedure. **The framing is corrected and the condition is not.** Being honest about an unrecoverable state does not make it recoverable, and RULING 4's boundary exists so the number of shops in that state stays at one until the escrow is real.
